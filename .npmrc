# Prevent hoisting to ensure workers get their own Vitest 3.2.x
# This is necessary because @cloudflare/vitest-pool-workers only supports Vitest 2.0.x - 3.2.x
# but the root uses Vitest 4.x
shamefully-hoist=false
public-hoist-pattern[]=*types*
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
