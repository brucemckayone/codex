# Neon Database Configuration Example
# Copy this to .env.dev and fill in your actual values

# Neon API Credentials (get these from Neon Console)
NEON_API_KEY=your_neon_api_key_here
NEON_PROJECT_ID=your_project_id_here
NEON_PARENT_BRANCH_ID=your_main_branch_id_here

# Database Connection Methods
# LOCAL - Use local Docker PostgreSQL
# EPHEMERAL - Use Neon Local Docker container
# BRANCH - Use Neon ephemeral branch (for testing)
# CI - Use Neon ephemeral branch in CI/CD
DB_METHOD=LOCAL

# Database URLs (set based on DB_METHOD)
# For LOCAL:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/main

# For EPHEMERAL (Neon Local):
# DATABASE_URL_PROXY=postgres://neon:npg@localhost:5432/neondb

# For BRANCH/CI (will be set automatically by branch manager):
# DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.build/neondb

# Example of what a Neon ephemeral branch URL looks like:
# postgresql://neondb_owner:password@ep-cool-name-123456.us-east-2.aws.neon.build/neondb?sslmode=require
