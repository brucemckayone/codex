{
  "name": "@codex/observability",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "vite build --config vite.observability.config",
    "dev": "vite dev --config vite.observability.config",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.3",
    "vitest": "^4.0.2"
  },
  "dependencies": {
    "vite-plugin-dts": "^4.5.4"
  }
}
