// Re-exporting core types from better-auth for convenience
// and to have a single source of truth for auth-related types.
export type { Session, User } from 'better-auth';

// We can define additional shared types here if needed.
