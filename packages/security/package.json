{
  "name": "@codex/security",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "vite build --config vite.config.security",
    "dev": "vite dev --config vite.config.security",
    "test": "vitest run --config vitest.config.security.ts",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "biome lint --write .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@codex/constants": "workspace:*",
    "@codex/database": "workspace:*",
    "@codex/observability": "workspace:*",
    "drizzle-orm": "0.44.7",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251014.0",
    "typescript": "^5.7.3",
    "vitest": "^4.0.2"
  }
}
