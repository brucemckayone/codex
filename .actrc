# Act configuration file for local GitHub Actions testing
# See .github/ACT_SETUP.md for usage

# Use medium Ubuntu image with Node.js, pnpm, and common tools
-P ubuntu-latest=catthehacker/ubuntu:act-latest

# Load secrets from .secrets file
--secret-file .secrets

# Load environment variables (for GitHub vars)
--env-file .github/.env.act

# Use custom event payload with correct default branch
--eventpath .github/workflows/event.json

# Set container architecture
--container-architecture linux/amd64
