/**
 * Type definitions for Content API Worker
 *
 * Re-exports shared worker types from @codex/shared-types for consistency.
 */

export type {
  AuthenticatedContext,
  Bindings,
  ErrorResponse,
  HonoEnv,
  SessionData,
  SuccessResponse,
  UserData,
  Variables,
} from '@codex/shared-types';
