/**
 * Type definitions for Identity API Worker
 *
 * Re-exports shared worker types from @codex/shared-types for consistency.
 */

export type {
  Bindings,
  Variables,
  SessionData,
  UserData,
  ErrorResponse,
  SuccessResponse,
  HonoEnv,
  AuthenticatedContext,
} from '@codex/shared-types';
