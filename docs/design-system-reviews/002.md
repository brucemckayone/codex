# Iteration 002 — 2026-04-18

**Status**: complete
**Trigger**: first cron iteration after `/loop 30min` was armed (iter-001 was manual)
**Cron**: `*/30 * * * *`, job `15266419`
**Files batch focus**: WebGL / shader-pipeline plumbing — the RAF-loop and GPU-lifecycle surface

## Files reviewed

| File | Kind |
|---|---|
| `apps/web/src/lib/components/ui/ShaderHero/ShaderHero.svelte` | existing; no iter-01 coverage |
| `apps/web/src/lib/components/ui/ShaderHero/jfa-sdf.ts` | new |
| `apps/web/src/lib/components/ui/ShaderHero/logo-texture.ts` | new |
| `apps/web/src/lib/components/ui/ShaderHero/webgl-utils.ts` | existing; JFA dependency |

**Excluded** (with reason): `shaders/*.frag.ts` files are pure GLSL template literals —
no Svelte/CSS/DOM surface. Out of scope for `/design-system`; belongs to
`/shader-preset-pipeline`. This observation fed back into a SKILL.md §1 patch (see below).

## Findings summary

| Severity | Count | Action |
|---|---:|---|
| Blocker | 0 | — |
| Major | 0 | — |
| Minor | 6 | 5 bd P2 tasks + 1 bd P3 task |
| Nitpick | 1 | Dropped (`mouse.active` stuck-true — intentional per shader renderer semantics) |
| Skill improvement | 4 | Applied directly + 1 audit-record task (P3) |

Lower signal than iter-001 (0 majors vs 6) — expected for a shader-plumbing batch. The
RAF/motion/GPU references carried the load; a11y/tokens/icons barely applied.

## Beads created

| ID | Priority | Title |
|---|:-:|---|
| `Codex-yovs` | P2 | RAF loop re-schedules even when reduced-motion / tab hidden |
| `Codex-gbwp` | P2 | Inline token fallbacks in ShaderHero mask broken wiring |
| `Codex-qt5w` | P2 | ShaderHero burst decay is frame-rate dependent |
| `Codex-1hwu` | P2 | JFA WebGL program cache has no disposal or handle validation |
| `Codex-3iib` | P2 | logo-texture.ts robustness: null ctx + no fetch timeout |
| `Codex-lbu6` | P3 | Unit tests for JFA pipeline + logo-texture pure helpers |
| `Codex-8vbg` | P3 | Skill patches applied from iteration 02 (audit record) |

All tagged `ds-review` + `ds-review-iter-02`.

## Skill patches applied (4 total — most productive iteration so far)

| Reference | Section | Motivation |
|---|---|---|
| `04-motion.md` | New §4 "Frame-Rate-Independent Motion (JS-driven physics)" — `dt` pattern + `Math.pow(decayPerSecond, dt)` idiom + RAF hygiene rules | Finding 5: `burstStrength *= 0.85` per frame is 120Hz/60Hz/30Hz-dependent; + Finding 1: RAF scheduled before visibility guard. Prior motion reference focused on CSS; JS-driven physics was undocumented. |
| `06-performance.md` | §10 new bullet 6: "Module-scoped GPU caches MUST expose disposal + validate handles" (`gl.isProgram()` + `destroy*(gl)`) | Finding 6: JFA program cache leaks across SPA nav and silently returns stale handles after external `gl.deleteProgram`. Generalises to any WebGL resource cache. |
| `06-performance.md` | §10 new bullet 7: "Enhancement fetches MUST be bounded" — `AbortController` + timeout + graceful null fall-through, with worked example | Finding 8: `loadLogoTexture` fetch hangs indefinitely on captive-portal / slow CDN; renderer never resolves. Pattern applies to any non-critical enhancement fetch. |
| `06-performance.md` | §10 new bullet 8: "Defensive `getContext` null-checks" — never non-null-assert `canvas.getContext('2d')!` | Finding 7-revised: privacy-hardened browsers (FF resistFingerprinting, Safari Lockdown) return null; `!` converts graceful fallback into runtime TypeError. |
| `SKILL.md` §1 | New "Out of scope" table routing GLSL/shader strings to `/shader-preset-pipeline`, service/worker code to `/backend-dev`, etc. | Reviewer observation: 3 of 6 candidate files were GLSL template literals with no design-system surface. Routing clarification saves future iterations from forced findings on misrouted files. |

Renumbered downstream sections in `04-motion.md`: old §4/5/6/7/8 → §5/6/7/8/9 to accommodate the new §4.

## Skill evaluation notes (from reviewer)

**References that pulled weight:**
- `04-motion.md` — RAF discipline + reduced-motion + now frame-rate-independence
- `06-performance.md` — resource lifecycle, graceful degradation
- `01-tokens.md` — token-fallback anti-pattern

**References not applicable this iteration** (explicitly named, not padded):
- `02-css-architecture.md` — 18 lines of scoped CSS, all correct
- `05-accessibility.md` — no ARIA/focus surface; canvas correctly `aria-hidden`
- `07-icons.md` — no icons
- `10-brand-editor.md` — files are downstream token consumers, not editor plumbing

**Balance observation**: when batch focus is GPU plumbing, motion + performance carry the
load. For balanced reference coverage across the full skill, future iterations should
alternate domains (iter-03 should target Svelte/CSS — pricing page tree, studio settings, or
existing UI primitives lacking coverage).

## Process learnings (fold back into §7 of SKILL.md if recurring)

- **"Out of scope" routing** should be a first-class part of the review: reviewer flagged
  this cleanly and the SKILL.md §1 table now prevents future wasted cycles. Iter-003 should
  apply the table before selecting files.
- **Minor-only iterations are still valuable** when they surface skill gaps. This iteration
  produced zero majors but 4 skill patches — the loop is capturing durable guidance even
  when the code is healthy.
- **Grouped bead creation** (Findings 7-revised + 8 both → one logo-texture robustness bead)
  improves task-list signal. Keep grouping when fixes share a file / context.

## Next iteration plan

Domain rotation: iter-003 should target **Svelte/CSS surface** to exercise the references
underused in iter-02. Candidate batches:

1. `routes/_org/[slug]/studio/settings/pricing-faq/` tree (new route — untouched by iter-01/02)
2. `routes/_org/[slug]/(space)/pricing/` page (reviewer noted heavy `color-mix()` usage; good stress test for `01-tokens.md` + `02-css-architecture.md`)
3. Existing long-lived components with no prior review: `SidebarRail.svelte`, `CommandPaletteSearch.svelte`, `FilterBar.svelte`

Pick 4–6 files across these. Give priority to files the iter-01 review mentioned as
containing pre-existing `--space-1-5` usage — verify the spacing-scale fix (if applied) landed
end-to-end.
