# Iteration 022 — 2026-04-23

**Mode**: user-directed scope expansion — investigate the per-theme feature request (`Codex-wwedk`) by mapping current dark-mode render chain (Agent I) and drafting schema + UX proposal (Agent J). Fired **manually** by user instruction, not by cron.

## Scope under review

**Agent I — darkOverrides render path trace (sonnet)**
- `packages/database/src/schema/settings.ts` (`branding_settings.dark_mode_overrides` column)
- `packages/platform-settings/src/services/branding-settings-service.ts`
- `workers/organization-api/src/routes/organizations.ts` (`fetchPublicOrgInfo`)
- `apps/web/src/routes/_org/[slug]/+layout.server.ts`
- `apps/web/src/routes/_org/[slug]/+layout.svelte`
- `apps/web/src/lib/brand-editor/css-injection.ts` (`DARK_VAR_PROPS`)
- `apps/web/src/lib/theme/tokens/org-brand.css` (dark-mode CSS rules)

**Agent J — wwedk schema + UX design (sonnet)**
- Full brand-editor-investigation README + token-registry.json
- `apps/web/src/lib/brand-editor/types.ts`, `brand-editor-store.svelte.ts`
- `packages/database/src/schema/settings.ts`

## Findings

blocker=0 major=1 (P1) design-artifact=1

| ID | Severity | Title |
|---|---|---|
| `Codex-lqvyy` | **P1 bug** | `darkModeOverrides` NEVER rendered for non-editor visitors — half-shipped feature |

**Artifact**: `docs/brand-editor-investigation/per-theme-tokens-design.md` — 459-line schema + UX proposal for `wwedk`. Recommends **Option B** (parallel `darkTokenOverrides` column).

Dependencies updated:
- `Codex-wwedk` now blocked by `Codex-lqvyy` (in addition to `Codex-9u8wg`). Per-theme feature requires the base render path to exist first as the template to extend.
- `Codex-wwedk` bead annotated with design doc pointer.

## Agent runs

- **Agent I** (sonnet, ~108k tokens, 2m): end-to-end trace of darkOverrides. Confirmed CSS consumers are correct; confirmed editor preview injection is correct; discovered the **SSR layout rendering path is completely absent**. Correctly identified this as a blocker for `wwedk`. Made the first substantive use of `/design-system` reference 10 in four iterations — reference's §10 gotcha list literally predicted the exact bug ("Brand editor preview works but server-rendered page doesn't").

- **Agent J** (sonnet, ~75k tokens, 3.7m): produced 459-line design doc at the specified path. Evaluated three schema options with concrete diffs, migration paths, CSS consumption patterns, and open questions for user. Recommended Option B with clear rationale (type safety / additive migration / future expansion). Also proposed a concrete rename: `darkOverrides` → `darkColorOverrides` in TypeScript to disambiguate now that a sibling concept is being added.

Both respected the no-MCPs rule. Both grounded claims in file:line citations.

## Verification performed before creating the P1 bead

This was a high-impact accusation ("half-shipped feature in production"). Verification was essential:

1. Read `+layout.svelte:281-282` — confirmed `darkModeOverrides` reference is inside the editor open `$effect` at line 277+ (editor-only path).
2. Widened grep across `apps/web/` for `--brand-color-dark|--brand-secondary-dark|--brand-accent-dark|--brand-bg-dark`:
   - **~50 READS** in `org-brand.css:215-265` (including OKLCH derivations for hover/active/subtle variants and text/border/surface for dark backgrounds).
   - **ZERO writes outside `css-injection.ts`** — the editor `$effect` path is the only writer, and it runs only when `brandEditor.open()` has been called.
3. No `$derived.by()` on `darkModeOverrides` anywhere in `+layout.svelte`.
4. No `style:--brand-color-dark={...}` binding anywhere in the layout.

Conclusion: Agent I's trace is correct. The bead is legitimate P1.

## Skill patches applied

**None this iteration.** Consistent with iter-018–iter-021. Agent J's design doc proposes concrete CSS patterns, TypeScript renames, and file touchpoints — these become skill patches *after* `lqvyy` + `wwedk` land.

However, Agent I explicitly recommended a patch to reference 10's §10 gotcha list: add `darkModeOverrides` as a named example of "brand editor preview works but server-rendered page doesn't". Queue this for after `lqvyy` lands (same discipline).

## Skill evaluation notes

- **Reference 10 is accurate and useful — just under-utilized.** For four iterations, agents skipped it and went straight to source. Iter-022 Agent I finally read it; the result was a faster trace AND a validated gotcha prediction. Future agent prompts for brand-editor-scope tasks should explicitly direct them to read reference 10 first.
- **Sonnet is correct for schema design work.** Agent J's 459-line doc includes TypeScript diffs, SQL migration pseudo-code, UX mockups, migration steps, and open questions. Haiku would underperform on the "recommendation with rationale" section and the trade-off analysis.
- **Auto-mode + user-directed priority change worked cleanly.** User said "go now" after adding the scope expansion; iter-022 fired manually (not cron-driven), ran two agents, produced both a major find and a design artifact, committed in one flow. The loop's flexibility to pivot on user direction is validated.
- **Design docs as iteration outputs are new.** iter-018 through -021 produced bugs + audit docs. Iter-022 adds a 459-line design proposal. The investigation README should probably grow a §7 "Design Documents" section to index these — multiple more are likely as more features are proposed.

## Convergence tracking

Previous trajectory: 2, 3, 1, 0 P1s (iter-018 → iter-021). Pause counter was 1/3.

**Iter-022 resets the counter.** Not because the loop isn't converging, but because:
1. User-directed scope expansion (`wwedk`) added new surface area to explore.
2. The exploration surfaced a confirmed P1 bug that had been latent in production.

The loop's pause condition is sensitive to "fresh investigation terrain". When the user expands scope, the counter resets. This is correct behavior — we shouldn't declare victory on a new investigation thread's first iteration.

New trajectory: 2, 3, 1, 0, **1** P1. Counter: 0/3.

## Next iteration seed (iter-023)

Two strong candidates:

1. **Audit the other dark-mode render paths for the same bug class.** `darkModeOverrides` is missing a render binding — are there OTHER per-theme JSON blobs in the branding pipeline that suffer the same bug? Candidates: is there any logic that should vary by `prefers-color-scheme` vs the `[data-theme]` attribute? Does `injectTokenOverrides` correctly apply light-mode tokenOverrides on its own without a dark equivalent, confirming shader behavior for non-editor users? Use sonnet — needs correlation across multiple files.

2. **Start pre-work for `Codex-lqvyy` fix**: NOT implementation (user hasn't authorized) but a concrete PR-ready sketch. Full patch diff for `+layout.svelte`. Unit test scaffolding for the new `$derived` blocks. Playwright test assertion that dark-mode visitors see dark override colors. Use sonnet — needs Svelte 5 rune precision.

Alternative: wait for user direction before starting (2). If `lqvyy` is prioritized for implementation by the user, iter-023 can spin up the PR. If user wants to keep the loop in investigation mode, pick (1) + a smaller item from the standard queue.

## Commit

Pushed as part of iter-022 commit (next in the main branch). Beads: `Codex-lqvyy` P1 + `Codex-wwedk` now blocked by `lqvyy`. Design doc: 459 lines. README updated.
