# Iteration 018 — 2026-04-23

**Mode**: targeted architecture investigation (not file-batch review) — loop redirected by user to diagnose user-reported brand editor bugs.

## Scope under review

Not files — the brand editor pipeline end-to-end:
- `apps/web/src/lib/brand-editor/` (11 files)
- `apps/web/src/lib/components/brand-editor/` (Mount + level panels)
- `apps/web/src/lib/remote/branding.remote.ts`
- `apps/web/src/lib/server/cache.ts`
- `apps/web/src/routes/_org/[slug]/+layout.{server.ts,svelte}`
- `workers/organization-api/src/routes/{settings,organizations}.ts`
- `packages/platform-settings/src/services/branding-settings-service.ts`
- `packages/database/src/schema/settings.ts`
- `packages/validation/src/schemas/settings.ts`
- `apps/web/src/lib/styles/tokens/*.css` + `apps/web/src/lib/theme/tokens/org-brand.css`

## Findings

blocker=0 major=2 minor=3 nitpick=1 (promoted to skill improvement / deferred = 0)

| ID | Severity | Title |
|---|---|---|
| Codex-ja9zp | major (P1 bug) | Brand save: CACHE_KV slug invalidation race |
| Codex-7afgp | major (P1 bug) | Brand save: no client `invalidate('cache:org-versions')` after save |
| Codex-g49b4 | minor (P2 bug) | 6 DB columns write-orphaned by editor |
| Codex-mdg94 | minor (P2 task) | Audit fine-tune panels for tokenOverrides routing |
| Codex-fopbo | minor (P3 task) | `BRAND_KV` vs `CACHE_KV` — document or consolidate |
| Codex-ag8l8 | nitpick (P3 bug) | CSS font value format inconsistency (preview vs SSR) |

Dependencies wired: `ja9zp` blocks `7afgp`, `g49b4` blocks `mdg94`.

## Agents (2 parallel, per feedback_agent_limits.md cap)

- **Agent A** (general-purpose, sonnet, ~125k tokens, 4m): produced full three-way diff table of ~80 tokens across CSS definition, editor control, DB schema, and CSS consumer. Correctly identified the 6 write-orphaned columns. Misidentified the glass-tint-dark gap (not a bug, intentional). Called out `lib/theme/tokens/org-brand.css` location as undocumented in skill.
- **Agent B** (general-purpose, sonnet, ~106k tokens, 3m): traced font save end-to-end across 8 hops. Correctly identified the waitUntil-based race as the likely root cause. Partially misdiagnosed the SvelteKit-side `invalidateCache(platform, orgId)` (it does bump orgId version, but slug version is the critical one — corrected in verification step).

Both agents respected the "no MCPs during audit" rule (§7) and stayed in static inspection mode.

## Verification performed before creating beads

Per `feedback_verify_before_declaring_unused.md`, I re-read:
- `apps/web/src/lib/remote/branding.remote.ts:150-280` to confirm `textColorHex` is still in the schema + forwarded to the worker (it is — Agent A was subtly wrong; the omission is at the Mount, not the remote).
- `apps/web/src/lib/components/brand-editor/BrandEditorMount.svelte:30-80` to confirm the payload omits fine-tune columns (confirmed — lines 49-69 explicitly scope the payload to primary identity fields + tokenOverrides JSON).
- `workers/organization-api/src/routes/settings.ts:90-205` to confirm the exact waitUntil composition (confirmed — all three invalidation tasks in `waitUntil(Promise.all(tasks))`).
- `apps/web/src/lib/server/cache.ts` to confirm `invalidateCache(platform, id)` is a thin one-version-bump wrapper (confirmed — it only writes to the id arg, does not resolve slug).

Net: beads are grounded in file+line citations, not agent summaries.

## Skill patches applied

**None this iteration.** The findings are implementation-specific (race between waitUntil and client reload; write-orphaned columns from a past migration). They do not generalize into design-system rules. The closest pattern candidate — "fire-and-forget cache invalidation creates races when the producer also consumes" — is a backend concern already covered by `workers/organization-api/CLAUDE.md` cache section, not a `/design-system` rule.

## Skill evaluation notes

- **Reference 09 (mcp-verification) correctly excluded from the audit prompt** per §7. Both agents treated MCPs as close-time tools, not research-time.
- **Reference 01 (tokens) gap**: the skill instructions at section 2 of the entry point say "under `apps/web/src/lib/styles/tokens/*.css` or `lib/theme/`" but `lib/theme/tokens/org-brand.css` is the operative org-brandable consumer, and it's not given its own bullet. Agent A's initial Glob missed it. **Candidate iter-019 skill patch**: add `lib/theme/tokens/org-brand.css` explicitly to the reference file list in `01-tokens.md` with a one-sentence note that it's the `[data-org-brand]`-scoped consumer of `--brand-*` tokens.
- **Reference 10 (brand-editor)**: was not opened by either agent in this iteration — they went directly to source files. Whether that's a sign it's stale or just more convenient to read code is worth watching. If iter-019 agents also skip it, consider demoting/merging.
- **Cross-skill gap**: the two-layer cache model (`BRAND_KV` blob vs `CACHE_KV` via `VersionedCache`) is documented in `workers/organization-api/CLAUDE.md` but not in `/caching` or `/design-system`. When a brand editor bug crosses into cache territory, an agent has to juggle both skills. Not blocking — just noting.

## Next iteration seed

Iter-019 should tackle two items from the investigation queue (see `docs/brand-editor-investigation/README.md` §4):

1. **Light/dark sync investigation** (high — user-reported, second-named bug). Agent scope: map every writer of `data-theme` / `color-scheme` / preference cookie across the brand editor toggle + the org sidebar toggle. Prove divergence with file+line citations. Produce a fix sketch. Use general-purpose sonnet.

2. **Cross-org brand injection on subdomains**. Agent scope: map `hooks.server.ts` reroute → `_org/[slug]/+layout.server.ts` → SSR vs CSR brand variable delivery. Identify any inheritance/override semantics between parent and child orgs. Use general-purpose sonnet or Explore.

Both should be spawned in parallel at the start of iter-019. No need to re-do iter-018's pipeline mapping — it's in the investigation README.
