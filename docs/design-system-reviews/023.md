# Iteration 023 — 2026-04-23

**Mode**: isolation-confirming sibling-bug hunt + PR-ready patch sketch. First iteration to produce a code-ready artifact rather than a design proposal or investigation map.

## Scope under review

**Agent K — Sibling-bug hunt for `lqvyy`-class defects (sonnet)**
- Pattern: per-theme or per-variant value, CSS consumer reads `var(--X)`, but `+layout.svelte` has no `style:--X` binding for non-editor users.
- Candidates checked: A) tokenOverrides light FOUC, B) all four `darkOverrides.{primary,secondary,accent,background}`, C) darkOverrides fine-tune keys, D) heroLayout per-theme, E) intro video theme-specific, F) dark logo swap, G) player tokens per-theme.

**Agent L — `Codex-lqvyy` PR-ready patch sketch (sonnet)**
- Full repro, diff, unit test, Playwright e2e, checklist, rollout plan.
- Written to `docs/brand-editor-investigation/lqvyy-patch.md`.

## Findings

blocker=0 major=0 minor=0 artifact-correction=0 skill-patch=1 pr-artifact=1

| ID | Severity | Title |
|---|---|---|
| `Codex-688ax` | skill (P3 task) | Add cross-reference rule: every `--brand-*` read by `org-brand.css` must be bound by `+layout.svelte` |

**Artifact**: `docs/brand-editor-investigation/lqvyy-patch.md` (520 lines) — PR-ready patch for `Codex-lqvyy`.

Dependencies: `Codex-688ax` blocked by `Codex-lqvyy` (reference reflects landed fix, not intent).

## Agent runs

- **Agent K** (sonnet, ~97k tokens, 2.5m): falsified the "lqvyy is the tip of an iceberg" hypothesis. Checked 7 candidates; 5 are N/A (concept doesn't exist or isn't per-theme by design), 2 are already tracked in `Codex-wcwpw` (tokenOverrides FOUC). Net: no new beads needed, `lqvyy` scope is already correct at 4 color fields, and the fix pattern should mirror existing light-mode bindings one-for-one.

- **Agent L** (sonnet, ~83k tokens, 3.3m): produced 520-line patch spec. Key engineering decisions:
  - Inline type for `darkModeOverrides` JSON shape rather than importing `ThemeColors` — avoids widening diff for a P1 hotfix.
  - Extract `parseDarkColorOverrides()` as a pure utility function — makes unit tests trivial; also reusable by `wwedk` later.
  - No CSS changes needed — `org-brand.css:215-265` already has `var(--brand-*-dark, fallback)` chains.
  - No feature flag — strictly additive, fallback-safe.

Both respected no-MCPs rule. Both grounded claims in file:line.

## Verification performed

Spot-checked:
- `lqvyy-patch.md` exists at expected path, 520 lines, opens with correct header.
- Agent K's file:line citations for `+layout.svelte:356-384` (style bindings), `types.ts:12-17` (ThemeColors), `css-injection.ts:362-367` (DARK_VAR_PROPS) — all accurate per iter-022's independent reads.
- Agent K's grep claim "logoUrlDark / darkLogo / logo_url_dark returned zero matches" is the kind of negative claim `feedback_verify_before_declaring_unused.md` warns about — but the claim goes the RIGHT way here (feature doesn't exist → no bug to file) rather than the dangerous way (feature exists but is dead → risk of deletion). Accepted without re-grep.

## Skill patches applied

**None this iteration.** Consistent with iter-018–iter-022 discipline. `Codex-688ax` is queued for after `lqvyy` lands — the cross-reference rule needs the fix to be the canonical example, not the intended one.

## Skill evaluation notes

- **Agent K's "rule of thumb" is the highest-leverage finding.** *"If `org-brand.css` reads it, the layout must bind it via `style:`"* — this one-liner would have prevented `lqvyy` at authoring time AND would shape how `wwedk` implementation extends the pattern. Added as `Codex-688ax` for a skill patch. This may eventually graduate to a hard rule (R15) if the pattern recurs.
- **Sonnet is the right tier for patch specs.** Agent L's engineering judgment (utility extraction, inline vs imported types, no feature flag) required real tradeoff analysis. Haiku would produce a correct diff but wouldn't optimize for downstream ergonomics.
- **The patch spec format is a useful iteration output.** Previous iterations produced bugs + audits + design docs. Iter-023 adds "PR-ready patch spec" to the repertoire. Worth repeating for other P1 beads that have a clear fix path (e.g., `Codex-oqv3r`'s one-line preset merge fix).

## Pause counter

Previous trajectory: 2, 3, 1, 0 (counter 1/3) → user expansion + iter-022 P1 (counter reset to 0/3) → iter-023: **0 P1** → counter **1/3**.

Note on counter semantics: iter-023 produced *no new bug beads*, but it produced a high-value implementation artifact (the patch). The counter tracks "investigative discovery", not "productive output". This is correct — the loop's reason to pause is when there's nothing new to discover, not when the work shifts from discovery to implementation.

If iter-024 and iter-025 also find zero new P1 bugs, the next fire should explicitly propose:
- Drop cron to weekly cadence (lower-frequency polls)
- OR pause entirely and restart when a specific bead is worked on

## Next iteration seed (iter-024)

Two items from the investigation queue, now sharpened by iter-023's confidence in scope:

1. **Preset category coherence audit** (investigation queue §4 item 6). 27 presets across 9 categories. Do each preset's `tokenOverrides` match the category's semantic label? E.g., Minimal presets bundling aggressive shaders would be incoherence. Use haiku — this is classification against a rubric.

2. **Intro video + logo upload flow mapping** (investigation queue §7). These are the two multipart-upload paths in the brand editor that iter-018–023 haven't examined. Map: editor control → upload endpoint → R2 storage → DB URL → render. Use sonnet — multi-layer synthesis.

Alternative: if user authorizes implementation of `lqvyy` between now and iter-024, iter-024 can verify the fix landed correctly (svelte-autofixer + Playwright via MCPs, per reference 09) rather than discovering new bugs.
