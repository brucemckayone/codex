# Iteration 020 — 2026-04-23

**Mode**: targeted architecture investigation + first use of **haiku** for pure-enumeration work, matching the user's directive to "use less intelligent models for gathering context about specific things".

## Scope under review

Two slices from iter-019's investigation queue:

**Agent E — Token registry JSON (haiku)**
- `apps/web/src/lib/styles/tokens/*.css` (13 files)
- `apps/web/src/lib/theme/tokens/*.css` (org-brand.css, light.css, dark.css)
- `apps/web/src/lib/brand-editor/types.ts`, `defaults.ts`, `css-injection.ts`
- `apps/web/src/lib/components/brand-editor/**/*.svelte`
- `packages/database/src/schema/settings.ts`
- `packages/validation/src/schemas/settings.ts`
- `packages/platform-settings/src/services/branding-settings-service.ts`

**Agent F — Preset round-trip audit (sonnet)**
- `apps/web/src/lib/brand-editor/brand-editor-store.svelte.ts` (`applyPreset`)
- `apps/web/src/lib/brand-editor/presets.ts` (27 presets)
- `apps/web/src/lib/components/brand-editor/levels/BrandEditorFineTuneColors.svelte`
- `apps/web/src/lib/components/brand-editor/levels/BrandEditorFineTuneTypography.svelte`
- `apps/web/src/lib/components/brand-editor/levels/BrandEditorShadows.svelte`
- `apps/web/src/lib/components/brand-editor/levels/BrandEditorHeroEffects.svelte`
- `apps/web/src/lib/components/brand-editor/levels/BrandEditorHeaderLayout.svelte`
- `apps/web/src/lib/components/brand-editor/levels/BrandEditorPresets.svelte`

## Findings

blocker=0 major=1 minor=0 nitpick=0 skill-improvement=1 data-fix=1

| ID | Severity | Title |
|---|---|---|
| `Codex-oqv3r` | major (P1 bug) | `applyPreset` wipes user fine-tunes — preset browsing destroys ~30 keys silently |
| `Codex-v4wao` | skill (P3 task) | Add "partial Record update" anti-pattern to `/design-system` reference 10 |
| `Codex-ac2o8` | data-fix (P3 task) | Minimal-style presets should set `shader-preset: 'none'` for explicit clearing |

Dependencies: both P3 tasks blocked by `oqv3r` (skill patch reflects landed fix; preset data fix only matters under merge semantics).

## Artifact

`docs/brand-editor-investigation/token-registry.json` — 2016 lines, 168 tokens. Machine-readable cross-reference of every defined token, its editor control (if any), its DB column (if any), and its `var(--...)` consumers. Produced by iter-020 Agent E in ~137k tokens / ~2.5m wall clock using **haiku**. Future iterations can query this file instead of re-walking the codebase.

## Agent runs

- **Agent E** (general-purpose, haiku, ~137k tokens, 2.5m): pure enumeration task. Produced well-formed 168-token JSON with category breakdown. Correctly identified 6 "zero-consumer" tokens as implicit OKLCH chain users (not bugs). Reinforced iter-018's `textColorHex` finding. Match between model class and task shape was ideal — haiku is the correct tool for structured-output enumeration.
- **Agent F** (general-purpose, sonnet, ~76k tokens, 1.8m): state-transition audit. Walked all 27 presets + 5 fine-tune panels to map key overlap. Produced a precise 3-scenario analysis, clear P1 severity call, and a one-line fix. Also caught a pattern-level anti-pattern worth capturing as a skill rule.

Both agents respected the "no MCPs during audit" rule.

## Verification performed before creating beads

Per session-standard:
- Read `apps/web/src/lib/components/brand-editor/levels/BrandEditorPresets.svelte:1-60` — confirmed single `onclick` handler with no confirm dialog, no warning. Line 28 is `onclick={() => brandEditor.applyPreset(preset)}`. Agent F's UX read validated.
- Read `brand-editor-store.svelte.ts:193-217` — confirmed exact code of `applyPreset` matches Agent F's quote. Lines 210-214 are the wipe.
- Counted tokens in `token-registry.json` via `python3 -c json.load` — 168 tokens present, 23 categories. Agent E's summary-message count of "118" was a mis-count of the summary block only; the tokens array itself is the full 168.

## Skill patches applied

**None this iteration** (but one queued for merge-time).

Consistent with iter-018 and iter-019: the reference patch for the "partial Record update" anti-pattern (`Codex-v4wao`) is blocked on `oqv3r` landing so the reference describes landed code, not intent. The anti-pattern text is drafted in the bead description — ready to paste into `references/10-brand-editor.md` when `oqv3r` merges.

Why defer: patching the skill with "do X" while the code does "not X" creates a trap where agents read the reference, trust it, and then grep for X and find nothing. Better to hold the patch until the code matches.

## Skill evaluation notes

- **Haiku for enumeration is a confirmed pattern**. User's original directive — "use less intelligent models for gathering context" — pays off here. Agent E's per-token work (definition line, consumer greps, control handler lookups) is mechanical: find X, cite Y. Haiku's cost profile (~1/4 of sonnet) + fast wall-clock + structured-output discipline match the task exactly. Future iterations with pure-enumeration scope should default to haiku.
- **Sonnet for synthesis + severity calls**. Agent F's job was analyzing state transitions, calling severity (P0/P1/P2?), designing fix options, and identifying pattern-level learnings. Haiku would underperform on severity hedging and the "partial Record update" generalisation. Keep sonnet for these.
- **No /design-system references opened this iteration** — both agents went directly to source. Third iteration in a row that reference 10 (`brand-editor.md`) wasn't opened. Flag for iter-021: either the reference is stale, or agents know the source well enough to skip it. Worth a read-pass to decide.
- **Loop is still producing majors**. Iter-018=2 P1, iter-019=3 P1, iter-020=1 P1. The §7 pause condition ("three consecutive zero-major iterations") is not close. Continue at 20m cadence.

## Cost-of-loop observation

Over three iterations (018, 019, 020), total agent usage is approximately:
- iter-018: 2 sonnet, ~231k tokens
- iter-019: 2 sonnet, ~150k tokens
- iter-020: 1 haiku + 1 sonnet, ~213k tokens (of which haiku ~137k)

Using haiku for enumeration lands iter-020 at similar total tokens as the sonnet-only iterations, but one of those tokens is at a much lower rate. The loop can sustain higher throughput by shifting enumerate-only tasks to haiku as they arise.

## Next iteration seed (iter-021)

Two items from investigation queue §4:

1. **Hero layout visibility flags audit**. Agent task: grep every `data-hero-hide-*` attribute writer + consumer. Confirm editor toggle writes correspond to layout render branches. Use sonnet (needs synthesis on where flags take effect).

2. **Zero-consumer token audit** (from `token-registry.json`). Agent E flagged `--color-brand-primary-hover`, `--color-interactive-hover`, `--color-brand-primary-active`, `--color-interactive-active`, `--color-brand-primary-subtle`, `--color-interactive-subtle` as having no direct `var()` consumers, theorised to be live via OKLCH relative color syntax. Agent task: grep for `oklch(from --brand-color` / `oklch(from --color-interactive` and confirm derivations cover these. If yes, add a note to the registry. If no, file as dead tokens. Use haiku (pure grep + classification).

Both in parallel at start of iter-021.
