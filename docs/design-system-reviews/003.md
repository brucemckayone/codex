# Iteration 003 — 2026-04-18

**Status**: complete
**Trigger**: cron `15266419` fire
**Batch focus**: Svelte/CSS surface (rotation from iter-02's GPU-plumbing focus)
**Outcome**: Rotation succeeded — 9 of 17 findings in 01/02/03/05 references vs iter-02's 04/06 weight

## Files reviewed

| File | Kind |
|---|---|
| `apps/web/src/routes/_org/[slug]/(space)/pricing/+page.svelte` (2585 lines) | modified |
| `apps/web/src/routes/_org/[slug]/(space)/pricing/+page.server.ts` | modified |
| `apps/web/src/routes/_org/[slug]/studio/settings/pricing-faq/+page.svelte` | NEW |
| `apps/web/src/lib/components/ui/FilterBar/FilterBar.svelte` | existing, underreviewed |
| `apps/web/src/lib/components/ui/ContentCard/ContentCard.svelte` | existing, cited-canonical in skill |
| `apps/web/src/lib/components/ui/ContentCard/SkeletonContentCard.svelte` | NEW |

## Findings summary

| Severity | Count | Action |
|---|---:|---|
| Blocker | 0 | — |
| Major | 8 | 5 new P1 bugs + 1 update to existing bead `Codex-lmex` |
| Minor | 8 | 1 consolidated P3 polish task |
| Nitpick | 1 | Dropped (ContentCard nested `<a>` card-wrap pattern — known idiom) |
| Skill improvement | 5 | Applied directly + 1 P3 audit-record task |

**Signal strength**: 8 majors is higher than iter-01 (6) and iter-02 (0). The Svelte/CSS
surface carries more UX-facing risk — a11y gaps are visible to real users in ways that shader
RAF economy isn't.

## Beads created / updated

| ID | Priority | Title |
|---|:-:|---|
| `Codex-lmex` | P1 | *(UPDATED)* `--space-1-5` broken outside `[data-org-brand]` — iter-03 adds FilterBar + SkeletonContentCard call sites |
| `Codex-y6em` | P1 | Pricing page has 123 `var(--*,fallback)` anti-pattern instances |
| `Codex-mfl7` | P1 | Reduced-motion bypasses on pricing and pricing-faq |
| `Codex-95na` | P1 | SkeletonContentCard dimensions drift from ContentCard → CLS on swap |
| `Codex-pv8j` | P1 | FilterBar radiogroup missing WAI-ARIA keyboard contract |
| `Codex-cc2n` | P1 | Missing `:focus-visible` rings in FilterBar and pricing-faq icon-btn |
| `Codex-6h61` | P3 | Pricing page polish batch (7 minor findings) |
| `Codex-7yh3` | P3 | Skill patches applied from iteration 03 (audit record) |

All new beads tagged `ds-review` + `ds-review-iter-03`.

## Skill patches applied (5 — iter-03 was the most productive skill-improvement iteration)

| Reference | Section | Motivation |
|---|---|---|
| `02-css-architecture.md` §4 | Fixed stale line anchor: `utilities.css:62-67` → `utilities.css:34-39` | Citation drift — file reshuffled between iter-00 utilities cleanup and iter-03 (finding 6) |
| `05-accessibility.md` §4 | New row in keyboard table + new subsection "Custom `radiogroup` keyboard contract" citing `handleBillingKey` in pricing as canonical | Finding 12 — FilterBar missed the WAI-ARIA radiogroup keyboard model. Melt UI has no radiogroup primitive; the pattern must be written down. |
| `01-tokens.md` §8 | New "Fallbacks at consumer sites vs at the declaration" subsection with pricing's 123-call-site evidence + "declare once at `:root`, consume bare" rule | Finding 2 — the anti-pattern iter-02 caught at 1 site now shown to be systemic at page-scale. The rule is: one declaration point, not many. |
| `01-tokens.md` new "Spacing scope cheat-sheet" | Explicit list of `:root`-available vs `[data-org-brand]`-only spacing tokens | Finding 1 — recurring across iter-00 + iter-03. Agents need a one-glance scope check before consuming unfamiliar steps. |
| `03-components.md` §9 (new) | "Skeleton Contract" — skeleton dimensions MUST match loaded component; preference for inline `{#if loading}` branch; shared-mixin if separate file needed | Finding 11 — SkeletonContentCard drifted from ContentCard on 5 dimensions. Class-of-bug worth preventing. |

Downstream section renumbers in `03-components.md`: §9 Compound Components → §10; §10 Gotchas → §11; §11 Re-verify → §12.

## Skill evaluation notes (from reviewer)

**Rotation succeeded**: iter-02 was 04+06 heavy (motion+perf); iter-03 landed primarily in
01+02+03+05 (tokens+architecture+components+a11y). All references now have live review data
except 07, 08, and 10 (which are genuinely not applicable to this batch's domain).

**Recurring patterns worth watching**:
- `--space-1-5` scope bug keeps appearing (iter-00, iter-03). Fix in `Codex-lmex` is load-bearing.
- Token-fallback anti-pattern keeps scaling up: 1 file in iter-02 → pricing-page-wide in iter-03. Next iteration should grep repo-wide for `var(--[a-z-]+,\s*[a-z0-9#])` to quantify the full blast radius.
- `:focus-visible` missing across multiple files (iter-00 BrandEditorHeaderLayout, iter-03 FilterBar + pricing-faq). The rule is universal; consider adding a repo-wide lint check (not a skill patch).

**References still underexercised**:
- `07-icons.md` — no findings across 3 iterations. Consider an iteration where the batch explicitly targets an icon-heavy component (e.g., VideoPlayer controls).
- `08-testing.md` — no findings; iteration agents inspect non-test files. An iter-N-test could review the test utilities themselves.
- `10-brand-editor.md` — only tangential findings (token-fallback is brand-editor-adjacent). A brand-editor-focused iteration targeting the editor panel components (BrandEditorLogoSettings, ColorPresets, etc.) would stress-test the path taxonomy.

## Process learnings

- **Bead update > bead duplicate**: iter-03 reviewer correctly flagged `--space-1-5` recurrences as additions to the existing `Codex-lmex` rather than duplicates. Future iterations should continue this. Consolidation preserves review throughput clarity.
- **Grep-wide patterns merit their own bead**: the pricing-page 123-instance fallback saturation (Finding 2) is one bead, not 123. Bead granularity = "one fix unit," not "one finding."
- **Skill patches compound**: iter-03's 01-tokens.md §8 extension builds directly on iter-02's initial anti-pattern callout, and iter-03's "fallback at declaration" rule is the actionable refinement. Three iterations of the loop produce a layered rule — this is the self-improvement working.

## Next iteration plan (iter-004)

**Domain rotation**: 07 and 10 are underexercised. Candidate batches:

1. **Icon-heavy domain** — VideoPlayer controls (`lib/components/VideoPlayer/`) + any recent changes to Icon/ components. Tests `07-icons.md` + `04-motion.md` (player motion).
2. **Brand editor panel domain** — `lib/components/brand-editor/levels/*` (excluding HeaderLayout which iter-00 covered). Tests `10-brand-editor.md` Path C (new editor level) compliance.
3. **Server + streaming domain** — `+page.server.ts` files across the tree; verify Shell+Stream pattern from `apps/web/CLAUDE.md`. Tests architectural rules not yet deeply covered.

Pick option (2) for iter-004 unless the cron fire happens during active work on (1) or (3).

## Cron status

`15266419` remains active. 5 of 7 available fires have landed (iter-001 manual + iter-002 + iter-003). Next fire in ~30 min will pick iter-004 batch automatically.
