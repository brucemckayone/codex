# Iteration 021 — 2026-04-23

**Mode**: targeted architecture investigation — **first convergence iteration** (zero new P1 findings). Loop shifting from "find new bugs" to "refine prior artifacts".

## Scope under review

Two slices from iter-020's investigation queue:

**Agent G — Hero visibility flags end-to-end audit (sonnet)**
- `apps/web/src/lib/components/brand-editor/levels/BrandEditorHeaderLayout.svelte`
- `apps/web/src/routes/_org/[slug]/+layout.svelte` (heroHideFlags derivation + emission)
- `apps/web/src/routes/_org/[slug]/(space)/+page.svelte` (CSS consumers)
- `apps/web/src/lib/brand-editor/css-injection.ts` (possible live-preview emitter)
- Full repo grep for `data-hero-hide`

**Agent H — Zero-consumer token classification (haiku)**
- 6 tokens flagged by iter-020 Agent E: `--color-brand-primary-{hover,active,subtle}`, `--color-interactive-{hover,active,subtle}`
- Directed search: `var(--token)` + OKLCH relative-color chain greps

## Findings

blocker=0 major=0 minor=1 nitpick=0 artifact-correction=2

| ID | Severity | Title |
|---|---|---|
| `Codex-rwci4` | minor (P3 bug) | `injectBrandVars` writes `--color-hero-hide-*` CSS vars with no consumers (noise) |
| `Codex-9225y` | artifact-correction (P3 task) | Correct `token-registry.json` — 6 tokens misclassified as zero-consumer (all LIVE-DIRECT) |
| `Codex-peqvl` | artifact-correction (P3 task) | Refine `Codex-wcwpw` scope — hero-hide is SSR-rendered, not FOUC |

No dependencies wired — all three are independent P3s.

## Agent runs

- **Agent G** (sonnet, ~62k tokens, 2m): traced all 5 `hero-hide-*` keys end-to-end. Confirmed ALL 5 chains are OK — zero orphans. Produced an important clarification: hero-hide is SSR-rendered via `$derived.by()` (runs on the server), not `$effect` (browser-only), so it is NOT affected by the FOUC that `Codex-wcwpw` documents. Surfaced a minor noise issue: `injectBrandVars` emits useless `--color-hero-hide-*` CSS vars during editor preview.

- **Agent H** (haiku, ~94k tokens, 1.2m): classified 6 tokens. Reported 4 LIVE-DIRECT, 2 DEAD. **The 2 DEAD claims were WRONG** — verification caught `--color-brand-primary-hover` consumed at `pricing/+page.svelte:2139` and `VideoPlayer/styles.css:291`, plus base theme definitions in `light.css:45-46` + `dark.css:45-46`. Haiku's "widened grep" discipline was insufficient for this particular search — needs an explicit `light.css`/`dark.css` pass next time.

## Verification performed before creating beads

Per `feedback_verify_before_declaring_unused.md`:
- Grep `color-brand-primary-hover|color-brand-primary-active` across `apps/web/` — found 2 live `var()` consumers (pricing, VideoPlayer) + theme defines in light.css + dark.css. **Caught Agent H's false-DEAD classification before it became a bead to delete production-used tokens.**
- Count `color-interactive-subtle` occurrences — 28 across 20 files, matching Agent H's "8+" claim (conservative).
- Did NOT file a "remove dead tokens" bead. Instead filed `Codex-9225y` to correct the registry.

Beads cite file:line per prior iterations' standard.

## Skill patches applied

**None this iteration.** Consistent with iter-018/019/020 discipline.

Agent G recommended adding a "Visibility toggles" subsection to reference 10 — but since there's nothing to fix in the hero-visibility chain, the subsection would document existing clean code. Deferring until something in that subsystem changes, OR until enough related context accumulates to justify a proper patch cycle.

## Skill evaluation notes

- **Hero visibility was under-budgeted for findings**. Sonnet delivered a thorough audit in 2 minutes, with zero P1 output. Similar "audit a subsystem that's probably fine" tasks should budget less (haiku + stricter output template) OR be deprioritized when the investigation queue has higher-leverage items.
- **Haiku needs a richer search template for "dead code" claims**. The widened-grep instructions in the Agent H prompt said "If you find ANY match anywhere... classify as LIVE-*" — Haiku followed this but still missed `pricing/+page.svelte:2139` and `VideoPlayer/styles.css:291`. Root cause: haiku's grep pattern didn't account for `var()` chains (`var(--X, fallback)`) where the token is the FIRST arg, not just the whole-file pattern. Adjustment for iter-022+: haiku dead-code prompts should explicitly include "check `var(--X[,` AND `var(--X ` AND `getPropertyValue('--X')` AND theme-file definitions (light.css/dark.css) as four distinct search patterns".
- **Reference 10 was mentioned but not opened** (fourth iteration in a row). I will read it next iteration to determine staleness.
- **`$derived.by()` vs `$effect` distinction deserves documentation**. Agent G's clarification — that the former runs in SSR and the latter doesn't — is the kind of subtle Svelte 5 pattern that bit `Codex-wcwpw`'s author. Worth a note in reference 03 (components) about when to use each for data-sourced-from-SSR derivations.

## Self-correction note

This is the first iteration where the loop corrected prior-iteration artifacts rather than discovering new surface area:
- `Codex-9225y` corrects iter-020's token-registry.json classifications
- `Codex-peqvl` refines iter-019's `Codex-wcwpw` scope

The self-correction is a healthy sign — the loop is cross-checking itself as more context accumulates. But it's also a convergence signal: the universe of UNDISCOVERED bugs is shrinking.

## Convergence tracking

Per /design-system §7: "Three consecutive zero-major-finding iterations → propose pause."

| Iteration | Major findings |
|---|---|
| 018 | 2 (P1 bugs) |
| 019 | 3 (P1 bugs) |
| 020 | 1 (P1 bug) |
| **021** | **0** ← pause counter: 1/3 |

If iter-022 and iter-023 also produce zero P1/P0, propose to the user: drop to weekly cadence OR pause the loop entirely (it lives only until session end anyway; 7-day auto-expiry is next Wed).

## Next iteration seed (iter-022)

Two items from investigation queue §4, picking medium-leverage candidates that should produce real findings:

1. **darkOverrides chain** — trace how `darkModeOverrides` is read at SSR layout level (not just editor preview). Given `Codex-9u8wg` will change editor preview mechanics (scope to `.org-layout[data-editing-theme]`), we need to verify dark-mode *rendering* for non-editor users still works. Use sonnet (synthesis on state model).

2. **Preset category coherence audit** — 27 presets across 9 categories (Professional/Creative/Bold/Minimal/Organic/Tech/Luxury/Playful/Atmospheric). Do each preset's `tokenOverrides` match the category's intent? E.g., Minimal presets bundling aggressive shaders would be an incoherence. Use haiku for the enumeration + a per-category coherence check (low synthesis — classify against rubric).

If both produce zero P1, this iteration log accumulates pause signals and next fire should explicitly propose dropping cadence.
