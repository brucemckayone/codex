# iter-018 — types × apps/web (Round 2, drift-detection)

> **Cycle**: types × apps/web, Round 2 cycle 6
> **Date**: 2026-04-26
> **HEAD before audit**: 50b8b94d (iter-017 commit)
> **Mode**: delta (since iter-006 commit a79bbcc3)

## §5 step 1 — Cell-due algorithm

```bash
git log a79bbcc3..HEAD --name-only --pretty=format: -- 'apps/web/src/**' \
  | grep -v '__denoise_proofs__' | grep -v '\.test\.ts$' | grep -v '/paraglide/' | sort -u
```

**Result: empty.** Zero production code modified in `apps/web/src/**` since iter-006 baseline (paraglide compiled output `apps/web/src/paraglide/messages/en.js` excluded — generated by paraglide build, not a human edit per the "shared generated-source files" feedback).

Working-tree audit: `git status --short -- 'apps/web/src/**'` shows only `apps/web/src/paraglide/messages/en.js` modified (generated). No human-edited TS/Svelte sources have changed.

## Cycle exit per §5.0

`types × apps/web`: zero churn. 8 open findings (Codex-lqvw4.12-19) carry forward unchanged from iter-006. Stop-criterion countdown: **1 of 3**.

R11 (cross-package type duplication) effectiveness check: not applicable for a no-churn cycle — no new code to regress against. Will assess on first churn-bearing Round 2 types × apps/web cycle.

## Round 2 status (after iter-018)

6 of 12 cells visited. **Types row first pass complete** (packages iter-016 ✓, workers iter-017 ✓, apps/web iter-018 ✓).

| Row | Cells visited in Round 2 |
|-----|--------------------------|
| security | 3 of 3 (iter-013/014/015) ✓ row complete |
| types | 3 of 3 (iter-016/017/018) ✓ row complete |
| performance | 0 of 3 |
| simplification | 0 of 3 |

## Skill patches applied

- (none).

## Next-cycle prep

Apply §5.0 sort `(open_findings DESC, last_run ASC)` with phase tie-break (security > types > performance > simplification):

| Cell | Open | Last run | Round 2? |
|------|------|----------|----------|
| performance × packages | 8 | iter-007 | NO |
| simplification × apps/web | 8 | iter-012 | NO |
| simplification × packages | 6 | iter-009 | NO |
| performance × workers | 5 | iter-008 | NO |
| performance × apps/web | 5 | iter-010 | NO |
| simplification × workers | 5 | iter-011 | NO |
| security × packages | 6 | iter-013 | YES (1/3) |
| types × packages | 6 | iter-016 | YES (1/3) |
| security × workers | 5 | iter-014 | YES (1/3) |
| security × apps/web | 5 | iter-015 | YES (1/3) |
| types × workers | 5 | iter-017 | YES (1/3) |
| types × apps/web | 8 | iter-018 | YES (1/3) |

Top tier (8 findings): performance × packages, simplification × apps/web. Phase priority breaks tie → **performance × packages** (iter-007 baseline `ce69f9c9`). Begins the performance row in Round 2.

Next cron fire: `performance × packages`.
