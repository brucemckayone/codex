# Triage — Master Status Board

> Generated and maintained by the `/triage` skill. Do not edit by hand outside of dismissing items.
> See `.claude/skills/triage/SKILL.md` for the workflow.

## ⚠️ Promoted routing rules

| Rule | Promoted | Source pattern | Verdict |
|------|----------|----------------|---------|
| RT1 | iter-004 | `signal:auto-loop-skip-rung-2-plus` | After 3 consecutive auto-loop /triage cycles with rung-1 yield ≤ 1, parent should pause /loop and surface a routing decision to the user. |
| RT2 | iter-006 | `route:self:proof-test-path-mechanical-fix` | Beads describing path/import/byte-mechanical fixes inside `apps/web/src/__denoise_proofs__/iter-NNN/` are auto-rung-1 (mechanical scaffolding fixes, not finding-resolutions). NEW iter cohorts of `__denoise_proofs__/` tests must inherit the corrected 5-dotdot `repoRoot` path: `resolve(__dirname, '../../../../..')`. The 6-dotdot mistake (resolves to `~/development/`, not the repo) is auto-fix material for any cycle that un-skips the test — file as a sidequest within the parent bead's commit, not as a separate bead. Lint candidate: grep `__denoise_proofs__/` for `'../../../../../..'` (6 dotdots). |
| RT3 | iter-010a | `signal:concurrent-cycle-suffix` | Concurrent /triage cycles MUST detect collision via working-tree state inspection (untracked `iter-NNN.md` OR uncommitted `master.md` / `recurrence.json` hunk citing `iter-NNN`). On collision, the SECOND agent adopts suffix `[a-z]` (`iter-NNNa`, `iter-NNNb`, …), picks the next-highest-priority bead the first agent has NOT claimed, does NOT touch master.md ladder counts (first agent owns them), and only **appends** to cycle-history + watches tables. Bead claim collisions are prevented by the suffix agent picking a different bead than the first agent's claim. Promoted iter-010a after 3 unanimous hits (iter-005a, iter-009a, iter-010a) with zero observed friction across all three cycle pairs. |

See `.claude/skills/triage/references/02-routing-rules.md` for full rule definitions.

---

## Ladder snapshot

<!-- LADDER START -->

_Snapshot timestamp_: 2026-04-29 13:50 (iter-012-apply: Codex-u498 verification 4-agent team executed 19/21 pass + 2 hard fails + 3 partials + 1 deferred-to-WP-11. 7 child beads filed, all wired as Codex-u498 dependencies. Codex-u498 stays open with `triage:routing:apply-partial` label, status reset `in_progress` → `open`. Rung-3 grows 21 → 28 — 7 NEW open beads added at rung-3 placeholder pending next /triage classify pass.)
_Eligibility filter_: excludes `denoise:*` only when **no** proof test path is cited — beads with cited proof tests in `__denoise_proofs__/` ARE resolution-ready and triage-eligible (per iter-005 correction). Excludes `ds-review*`, `fallow-followup` (owned by other skills) and in-progress beads + Codex-ttavz.12 (routed iter-001) + closed beads (Codex-fcdkk iter-002, Codex-y6x9j iter-003, Codex-zhe80 iter-005, Codex-v5bzy iter-005a-apply, Codex-w30gi+Codex-0n26b+Codex-mqyql.18 iter-006, Codex-tj03p iter-007, Codex-mm0z9 iter-008, Codex-y63gl.14 iter-009, Codex-d3g6 iter-009a-apply, Codex-3u505 iter-010). Codex-6axi0 (just verified, blocked by Codex-zp30d) excluded per cycle context.

| Rung | Count | Top by priority |
|------|-------|-------------------|
| 0 — Trivial | 0[^iter007-r0] | — |
| 1 — Mechanical | 0[^iter009-r1] | — |
| 2 — Scoped | 0[^iter010-r2] | — |
| 3 — Multi-file | 28[^iter010-r3][^iter011-r3][^iter012-r3][^iter012-apply-r3] | Codex-x0pa (P0, walk iter-011, awaiting greenlight), Codex-6axi0 (P1, blocked), Codex-i49f (P1, blocked by Codex-u498), Codex-u498 (P1, **blocked by 7 children post iter-012-apply**), Codex-nwlhm (P2 NEW, root-auth bug), Codex-rf1l9 (P2 NEW, reduced-motion a11y), Codex-peuan (P3 NEW, arrow-keys), Codex-1k9ns (P3 NEW, studio-expanded), Codex-onjxy (P3 NEW, rail-width-token), Codex-usqgu (P3 NEW, palette-cross-org-goto), Codex-95vdy (P4 NEW, logout-wording), … Codex-70xgd (P3, needs-design, reclassified iter-010) |
| 4 — Design-needed | 5 | Codex-ev3k (P1 epic), Codex-cbbet (P2 epic), Codex-zp30d (P2), Codex-r4woq (P2 epic), Codex-84b53 (P2 epic) |

[^iter007-r0]: iter-007 picked Codex-tj03p (P4 stale-doc bug, rung-0 case-(c) per R10). Bead description was self-contradictory — claimed `routes/_org/[slug]/studio/settings/+layout.svelte:15` imports `SETTINGS_NAV` from `$lib/config/navigation`, but grep shows zero references to `SETTINGS_NAV` anywhere in the codebase (the symbol does not exist; doc previously cited a phantom array). The settings layout's tabs ARE genuinely hardcoded — confirmed by reading the file. Doc was rewritten to drop the phantom-`SETTINGS_NAV` parenthetical and explain WHY the tabs live locally (i18n-keyed labels + bespoke value/href shape). No test (case (c) — pure docs). Reclassified Codex-mm0z9 from rung-3 (iter-006 snapshot implicitly bucketed it there) to rung-1 — bead is a clean grep-and-replace recipe, 120 byte-identical sites, but kept at rung 1 not picked this cycle (R1 — one bead per cycle).

[^iter009-r1]: iter-009 picked Codex-y63gl.14 (P1 perf hot-path, rung-1 case-(b) per R10). Lifted ShaderHero's 30-frame poll-counter (ShaderHero.svelte:135-146) into a shared closure-factory helper at `apps/web/src/lib/components/ui/ShaderHero/use-poll-config.ts`. Both ImmersiveShaderPlayer.svelte (renderFrame:171) and ShaderPreview.svelte (renderFrame:80) now build their own `pollConfig` closure and call it per frame instead of `getShaderConfig(null, ...)` directly. Pre-fix: 60 forced style recalculations/sec per active immersive player. Post-fix: ≤ 2/sec (1 init + 1 per ~30 frames). R10 case (b): rewrote `apps/web/src/__denoise_proofs__/iter-010/F1-shader-config-hot-path.test.ts` from `describe.skip` sketch into 4 behavioural assertions (default cadence ≤6 calls in 120 frames, cache identity between refreshes, custom cadence, lexical-state pickup for switchPreset). All 4 pass. ShaderHero.svelte not migrated — its inline block has tight coupling to the local `presetOverride` prop and is documented inline; migration would be a no-op functionally. Pre-existing typecheck errors elsewhere unaffected. Backlog rung-1 still 0 after this cycle — RT1 will re-fire on next loop unless /denoise files new mechanical work.

[^iter008-r1]: iter-008 picked Codex-mm0z9 (P3 CSS token sweep, rung-1 case-(c) per R10). Pre-flight verified the recipe was genuinely byte-identical: 120 sites in 71 files, all in `:focus-visible` rules, all preceded by a uniform `outline:` declaration, `--space-0-5` token confirmed at `apps/web/src/lib/styles/tokens/spacing.css:7` (= `2px * density`, so byte-equivalent at default density=1 with density-aware scaling preserved). Sweep applied via `sed -i ''` over `grep -rl` results (single-file pass) plus a follow-up sed for the 2 `!important` cases in StudioSidebar/StudioSwitcher. Stash-and-clean executed because user had pre-existing concurrent work in some target files; final diff is 71 files / 120+ / 120-, 1:1 mapping. Grep guard PASSES: 0 occurrences of `outline-offset: 2px` remain. R10 case (c): pure CSS token swap, visual equivalence is mathematical (token resolves to identical value at canonical density). Pre-existing typecheck errors (iter-029 F3 unrelated, studio-access.svelte.test.ts from cf14d58a) unaffected. Backlog now has zero rung-0 AND zero rung-1 items — auto-loop /triage will trip RT1 again on next cycle unless /denoise files new mechanical work.

[^iter006-r1]: iter-006 was the FIRST cluster-defect agent-team cycle (R1 exception). 3 parallel sub-agents resolved F1 (Codex-w30gi)/F3 (Codex-0n26b)/F4 (Codex-mqyql.18) — all sharing the iter-002 `repoRoot` 6→5 dotdot path bug plus each bead's distinct rung-1 code-fix recipe. 14 proof assertions pass across the 3 tests. `route:self:proof-test-path-mechanical-fix` promoted to **RT2** after 3rd hit. Rung 2 cleared by concurrent iter-005a-apply (Codex-v5bzy). Backlog now exclusively rung-3+ work — auto-loop /triage will start tripping RT1 again unless /denoise files new mechanical beads.

[^iter009a-apply]: iter-009a-apply closed Codex-d3g6 (P1 multi-file test-coverage bead, rung-3 case (b) per R10). User chose Option A (One PR) at iter-009a's walk-phase routing question. Single sub-agent wrote 31 tests across 4 files: `enforce-policy-inline.test.ts` (+3 invalid-UUID rejection cases on the query-param path — closes the narrow remaining gap on sub-item 1, which was *partially stale* per the walk's pre-flight grep), `transcoding-service.test.ts` (+12 tests across 3 surfaces: `handleProgressWebhook` 5 tests, `triggerJobInternal` async-refactor 4 tests, `handleWebhook` fallback-mediaId+progress-clearing 3 tests), `subdomain.test.ts` (+10 tests across `buildOrgUrl` 6 + `buildCreatorsUrl` 4), `validation/transcoding.test.ts` (+6 tests on `runpodWebhookUnionSchema` discriminated-union dispatch, plus 1 bonus range-check = 7 in block). All 4 test files run independently with 100% pass: 34/34 + 21/21 + 21/21 + 25/25 = 101/101 total tests passing. Pre-existing tests in each file continue to pass — no shared mutable state was introduced. Strict no-touch rule honoured: zero production-code edits. Rung-3 backlog drops 21 → 20 (Codex-d3g6 closed). `signal:bead-description-partially-stale` verdict_history extended with `auto-resolve` entry tagged `apply-option-a`; hits stays at 1 (same bead).

[^iter010-r2]: iter-010 drained the rung-2 queue: Codex-3u505 **closed as already-shipped duplicate** of Codex-lfx11 (commit `b566eabd`, 2026-04-25). Codex-3u505 was filed 2026-04-24 00:25 citing `ContentAccessService.integration.test.ts:~310`; Codex-lfx11 was filed 2026-04-24 15:28 (same surface, same defect) and resolved one day later in `b566eabd` titled "fix(access): align paid-content fallback test with management-only role narrowing (Codex-lfx11)" — body explicitly says *"Fix path (a)"*, byte-for-byte Codex-3u505's option (a). The test was renamed to "for management members (fallback)", fixture seed changed `'member'` → `'creator'`, comment block added citing the f585f835/8b40f190 narrowing. `bd close` cited b566eabd + Codex-lfx11 as resolver. **No user gate** because zero-byte diff is audit-trail cleanup, not a code change (same shape as iter-007 reinterpretation). New ledger fingerprint `signal:bead-resolved-by-sibling-already-shipped` (hits=1) and (under earlier walk-phase agent) `signal:bead-fully-stale-already-resolved-by-sibling` — same shape, two entries that should consolidate next promotion review.

[^iter010-r3]: iter-010a (concurrent walk-phase agent) reclassified Codex-70xgd rung-2 → **rung-3 + needs-design**. Bead body is 3 lines, cites no `file:line` (R8 fires). Pre-flight verification: (1) `packages/cache/src/cache-keys.ts` has NO `COLLECTION_USER_FOLLOWING(userId)` cache type today, only `COLLECTION_USER_LIBRARY` and `COLLECTION_USER_SUBSCRIPTION` are tracked in the client manifest; (2) `apps/web/src/lib/client/following.svelte.ts` is a pure-localStorage Svelte 5 store with no server version key tracked; (3) the org layout (`_org/[slug]/+layout.svelte:281-291`) explicitly documents the trade-off: *"Cross-device 'followed on another device' drift is accepted as a trade-off"*; (4) `workers/organization-api/src/routes/followers.ts` POST/DELETE call `invalidateUserLibrary` only — no per-user-following collection version exists. Implementation surface is ~9 files across @codex/cache + organization-api worker + apps/web layout (cache-keys.ts, helpers/invalidate.ts, index.ts, invalidate-helpers.test.ts, followers.ts + tests, _org/[slug]/+layout.{server.ts,svelte}, following.svelte.ts + tests). Bead self-flags as "Optional — current design is intentional trade-off"; the load-bearing question is "do we still want the trade-off, or is now the time to invest?" — that's a design call, not a mechanical fix. Bead labelled `triage:rung-3` + `triage:iter-010` + `triage:needs-design`. Codex-3u505 closed (rung-2 -1) AND Codex-70xgd reclassified (rung-2 -1, rung-3 +1) net rung-2: 2 → 0, rung-3: 20 → 21.

[^iter012-r3]: iter-012 picked Codex-u498 (P1 nav-redesign verification, rung-3 walk-only — `--bead=Codex-u498` forced because user excluded Codex-x0pa from this cycle, Codex-6axi0 blocked by Codex-zp30d, Codex-i49f blocked by Codex-u498). Pre-flight gate passed: status=open, owner=brucemckayone@gmail.com, dependency Codex-nn8l **closed** 2026-04-08, no `triage:in-progress-other-agent` label. **CLAIMED status=in_progress immediately after pre-flight gate** per cycle's stricter protocol — supersedes RT3 working-tree fingerprint as collision lockout. Surface map (per `docs/nav-redesign/WP-10-verification.md`): 10 DevTools manual tests (T-01..T-10) + 11 Playwright automated checks (PW-01..PW-11) = 21 total assertions. Components touched: `apps/web/src/lib/components/layout/SidebarRail/` (3 files), `MobileNav/` (3 files), `StudioSidebar/` (4 files), `command-palette/` (1 file), `search/CommandPaletteSearch.svelte` + integration into platform layout (`(platform)/+layout.svelte:11,89`) and org layout (`_org/[slug]/+layout.svelte`). View-transition wiring (WP-09) confirmed live: `view-transition-name` cited in SidebarRail.svelte + StudioSidebar.svelte + ImmersiveShaderPlayer + ShaderHero + org layout. **Coverage gap finding**: ZERO existing E2E specs reference any of the new nav components (`MobileBottomNav`, `SidebarRail`, `CommandPalette`, `Main navigation`, `Mobile navigation` — all return 0 hits across `apps/web/e2e/`). The `studio/navigation.spec.ts` spec exists but uses `.studio-layout` selector — different surface; doesn't cover the new platform/org nav redesign. Zero component-test coverage for the 11 new component files (no `*.svelte.test.ts` siblings). Bead is therefore NOT partially stale (vs Codex-x0pa) and does NOT cite a nonexistent feature — every component referenced exists. T-08 brand editor coexistence check uses `?brandEditor` query param convention. 4 routing options framed: (A) one bundled verification PR with all 21 checks running via chrome-devtools+playwright MCPs in this cycle, (B) split into 4 child beads matching the team's per-surface convention (Codex-u498.1 sidebar-rail desktop, .2 mobile bottom-nav + sheet, .3 command-palette, .4 a11y/responsive/auth/transitions), (C) spawn `/backend-dev` or `/design-system` for re-execution via skill MCP gates, (D) defer until after Codex-i49f's review pass. Bead labelled `triage` + `triage:rung-3` + `triage:iter-012`. NO routing label until user answers. Ladder counts UNCHANGED — bead stays rung-3 + status=in_progress until next session's apply pass closes it. NEW ledger fingerprint `signal:bead-is-test-plan-execution` (hits=1) — distinct from existing test-related signals because the bead is NOT a missing-test-coverage bead (`signal:bead-description-partially-stale`) NOR a misclassified verify-only bead — it is the canonical "execute this test plan" shape that nav-redesign WP-10 represents. `signal:bead-description-partially-stale` NOT bumped (zero existing E2E coverage for these surfaces); `signal:bead-cites-nonexistent-feature` NOT bumped (every cited component exists).

[^iter012-apply-r3]: iter-012-apply executed user-clarified Option A (4 parallel sub-agents, each owning one slice, confirming at end — user explicitly waived per-PR commit/atomicity discipline because "no production app, hardening phase, its no big deal"). NEW fingerprint `signal:cluster-execute-heterogeneous-slices` at hits=1 — distinct from §13 cluster-defect (byte-equivalent recipes) because slice 1 desktop / slice 2 mobile / slice 3 palette / slice 4 a11y-cross-cutting recipes are non-byte-equivalent but share the verification-execution fingerprint family. 4 background Agents fired in single message: slice 1 (6 checks, all PASS — including 200ms hover-expand timing, glass morphism + rounded-right-corners on hover, org-branded `--rail-glass-bg` derived from `--color-surface`, `aria-current="page"` correctly applied), slice 2 (5 checks, all PASS — including synthesised `TouchStart → TouchMove × 10 (Δ200px) → TouchEnd` confirming the `>100px → open=false` drag handler fires correctly), slice 3 (T-03 9-sub + PW-03 = 11 sub-checks, all PASS — measured 301ms debounce within 250-400ms window matching 300ms spec, org-scoped search via `?scope=studio-alpha` query param confirmed 3 org-scoped vs 5 mixed-org results), slice 4 (8 checks: 4 PASS + 2 FAIL + 3 PARTIAL — see fails). Aggregate: **19/21 pass-or-deferred + 2 hard fails + 3 partials + 1 expected-pending-WP-11**. Hard fails: PW-10 (platform `/` does NOT render auth state in SidebarRailUserSection — `/discover` and `/library` show authenticated avatar with same session, but `/` shows Sign In. SSR HTML for `/` contains `user-section-unauth` marker; layout-level data-flow gap, likely `(marketing)` layout doesn't propagate `locals.user`) and T-10.reduced-motion (nav redesign components missing `@media (prefers-reduced-motion: reduce)`; StudioSidebar* honours it — copy that pattern for SidebarRail + MobileBottomNav + MobileBottomSheet + CommandPalette + CommandPaletteSearch; WCAG 2.3.3 implicated). Partials: T-06 tooltip lazy-render (Melt UI pattern, not implementation bug — tooltip renders on focus not hover-over-icon), T-07 studio sidebar uses 64→260 hover-expand instead of always-expanded per spec (drift OR bug, design clarification needed before fix lands), T-10.keyboard arrow keys do NOT navigate within rail (Tab works fine, ArrowDown/Up don't move focus — roving-tabindex pattern needed). Expected-pending Codex-i49f: T-09 at 768px on `/creators` (legacy `Header/UserMenu` + `Header/MobileNav` still imported in `_creators/+layout.svelte:12-13` per F2 of iter-012 walk doc — defer to WP-11 cleanup, NOT a u498 fail). 7 child beads filed: Codex-nwlhm (P2 bug, root-auth — likely rung-1 with locals.user propagation fix), Codex-rf1l9 (P2 bug, reduced-motion — rung-1 CSS @media addition), Codex-peuan (P3 task, arrow-keys), Codex-1k9ns (P3 task, studio-expanded — needs design clarification, possibly rung-3 needs-design), Codex-onjxy (P3 task, rail-width-token), Codex-usqgu (P3 bug, palette cross-org goto missing buildContentUrl), Codex-95vdy (P4 task, "Log out" vs "Sign Out" wording). 7× `bd dep add Codex-u498 <child>` wires bidirectional blocking. Codex-u498 status reset `in_progress` → `open`, label `triage:routing:apply-partial` added. R3 honoured (rungs 3-4 never auto-resolve to close). **Recurrence ledger updates**: `route:self:promoted-helper-missed-call-site` bumped 2 → 3 (PROMOTION ELIGIBLE — third missed-helper call-site recurrence: `invalidateOrgSlugCache` iter-003, `pollConfig` iter-009, `buildContentUrl` iter-012-apply); `signal:bead-is-test-plan-execution` verdict_history extended (hits stays at 1 — same bead, multiple verdict_history entries don't increment 3-distinct-bead promotion threshold per `signal:type-drift-template-vs-api` precedent); 2 NEW patterns at hits=1 — `signal:cluster-execute-heterogeneous-slices` (4-agent team with non-byte-equivalent recipes targeting single bead, distinct from §13 cluster-defect) and `signal:apply-phase-partial-children-block-parent` (parent stays open with N children blocking close, distinct from clean-close apply phases like iter-009a-apply / iter-005a-apply). Total wall time ~12 min for the 4-slice team-execute (slowest slice 4 at 11.5 min). Memory-feedback note: user explicitly overrode `feedback_agent_limits.md` 2-agent cap for this cycle ("we can have 4 subagents doing each seperatly") — RT-candidate cluster cycles are exempt from the default cap.

[^iter011-r3]: iter-011 picked Codex-x0pa (P0 E2E subscription verification, rung-3 walk-only — `--rung=3` forced). Default picker landed at top of rung-3 by `(priority DESC, age DESC)`. Pre-flight gate passed: status=open, owner=current user, dependency Codex-eejz **closed** 2026-04-08, no `triage:in-progress-other-agent` flag. Surface map: 10 sub-flows × ~12 frontend pages × ~4 backend packages × 2 workers × ~3 fixture files; 8 existing subscription specs in `apps/web/e2e/` (291 + 186 + 254 + 201 + 314 + 273 + … LOC) cover steps 9 + 10 already (`account-subscription-cancel.spec.ts`, `subscription-cross-device.spec.ts`); 4 specs are `test.skip` behind blocker Codex-z1wuz (synthetic `stripeSubscriptionId` 404s on real Stripe API calls). **Critical pre-flight finding**: Step 8 ("Free for me toggle") returns ZERO grep hits across `apps/`, `packages/`, `workers/` — feature does not exist; bead is the only place in the codebase that mentions the concept. 4 routing options framed: (A) one monolithic spec (NOT recommended; diverges from team's per-flow convention), (B) split into 4 child beads matching team convention (RECOMMENDED — file Codex-x0pa.1 stripe-connect, x0pa.2 tier-mgmt, x0pa.3 purchase-flow, x0pa.4 free-for-me product-decision), (C) spawn `/backend-dev`, (D) defer. Bead labelled `triage` + `triage:rung-3` + `triage:iter-011`. NO routing label until user answers. Ladder counts UNCHANGED — bead stays rung-3 open. New ledger fingerprint `signal:bead-cites-nonexistent-feature` (hits=1). `signal:bead-description-partially-stale` bumped 1 → 2 (Codex-d3g6 + Codex-x0pa) — one bead from 3-distinct-bead promotion threshold.

<!-- LADDER END -->

---

## Cycle history

| Iter | Date | Rung | Bead | Action | Outcome |
|------|------|------|------|--------|---------|
| iter-001 | 2026-04-27 | 3 | Codex-ttavz.12 | routed to /backend-dev (wire-up planned functionality) | bead remains open with labels triage:routing:backend-dev + triage:needs-design; user will invoke /backend-dev next session to wire forms + fix typo |
| iter-002 | 2026-04-27 | 1 | Codex-fcdkk | mechanical: corrected `repoRoot` path in 5 iter-012 proof tests (6→5 dotdot levels) | closed; tests collect cleanly + repoRoot now resolves to actual repo root (verified via node fs check) |
| iter-003 | 2026-04-27 | 1 | Codex-y6x9j | mechanical: replaced inline slug-resolve+invalidate block in `workers/organization-api/src/routes/settings.ts` with existing `invalidateOrgSlugCache` helper from `@codex/worker-utils` (R14). Re-classified from rung-4 (iter-002) to rung-1 — earlier classifier matched a `.env` regex on description text; reread shows pure mechanical helper-extract. | closed; F2-dup-slug-resolve-invalidate proof test extended to include settings.ts and passes (1/1); existing settings.test.ts passes (24/24); typecheck clean |
| iter-004 | 2026-04-27 | n/a | (none) | re-scanned rung-3 P2 (15 beads) + P1 (5 beads) clusters per iter-003 misclassification precedent; all confirmed-not-rung-1. Recurrence `signal:auto-loop-skip-rung-2-plus` hit 3rd time → **promoted to RT1**. | `ok: false`, no rung 0/1 work auto-resolved; new hard rule codified |
| iter-005 | 2026-04-27 | 1 | Codex-zhe80 | mechanical: replaced inline `interface SocialLinks` and `interface ContentItem` declarations in `apps/web/src/lib/components/ui/CreatorCard/CreatorCard.svelte` with `import type { ContentItem, SocialLinks } from './types'` (canonical sibling exports the identical shapes). Also fixed the proof test's `repoRoot` path (6→5 dotdots, same shape as iter-002's iter-012 fix) and un-skipped `describe.skip` → `describe`. Brief over-rode iter-004's denoise:* filter — denoise-tagged beads with cited proof tests are resolution-ready. | closed; F2 proof test passes 5/5; identified F1/F3/F4 sibling proof tests have the SAME path bug → iter-006 agent team queued |
| iter-005a | 2026-04-27 | 2 | Codex-v5bzy | parallel cycle to iter-005 (concurrent agent claimed rung-1 Codex-zhe80; this cycle picked next-lowest non-empty rung per user policy 0–3). Read-only walk produced two candidate-diff sketches: (a) narrow Spotlight's creator type to `{name?}` matching the canonical `ContentWithRelations.creator` shape (~3 lines, 1 file, single-package); (b) extend canonical `ContentWithRelations.creator` to include `username/displayName/avatar` plus content-api JOIN (~30+ lines, 3+ files, cross-package, possibly rung-4 if user schema lacks the columns). Bead labelled `triage:rung-2` + `triage:needs-greenlight` + `triage:iter-005`. | needsUser; bead reverted to `status=open` for next cycle. Awaiting user choice between option (a) / (b) / skip / reroute. |
| iter-005a-apply | 2026-04-27 | 2 | Codex-v5bzy | apply pass after user greenlight on option (a). Narrowed `SpotlightItem.creator` to `{name?: string \| null}`, rewrote `creatorName` derivation to `item.creator?.name ?? ''`, pruned now-unreachable `<AvatarImage>` block (the `avatar` field is no longer on the type; AvatarFallback initial-letter avatar takes over — no UX regression vs pre-fix because the entire creator block was hidden anyway), and removed unused `AvatarImage` import. Proof test added at `apps/web/src/lib/components/content/Spotlight.svelte.test.ts` asserting (1) creator name renders for `{name: 'Jane Doe'}`, (2) block hidden when name is null, (3) block hidden when creator is absent. | closed; proof test passes 3/3; web typecheck clean for Spotlight (pre-existing iter-029 F3 typecheck error unrelated, committed in ead3f9db). |
| iter-006 | 2026-04-27 | 1 (TEAM) | Codex-w30gi + Codex-0n26b + Codex-mqyql.18 | **First cluster-defect agent-team cycle (R1 exception per §13).** 3 parallel sub-agents fixed the shared `repoRoot` 6→5 dotdot bug across F1/F3/F4 iter-027 proof tests + each bead's distinct rung-1 code-fix recipe. F1: replaced 2 inline `STUDIO_ROLES` triples with `useStudioAccess()` composable (SidebarRail.svelte + SidebarRailUserSection.svelte). F3: extended `$lib/server/content-detail.ts` with `EMPTY_SUB_CONTEXT` + `DENIED_ACCESS_RESULT` constants; both content detail loaders now spread these. F4: 1-line text edit in gitignored `references/05-domain-web.md` (`loadFromServer()` → `hydrateIfNeeded()`). Pattern `route:self:proof-test-path-mechanical-fix` promoted to **RT2** after this cycle. | closed (3 beads); 14 proof assertions pass (6+4+4); §13 prototype validated — surface gaps: (1) need `localOnlyFilesChanged` field for gitignored fixes, (2) recipe wording "ensure-or-create" instead of "create" when helper may already exist. |
| iter-007 | 2026-04-28 | 0 | Codex-tj03p | rung-0 stale-doc fix (R10 case (c)): `docs/pricing-page/phase-3-studio-ui.md:154` rewrote the parenthetical that referenced a phantom `SETTINGS_NAV` array (zero refs in codebase per grep). New text explains the settings-layout tabs are intentionally local (i18n-keyed labels + bespoke value/href shape that doesn't fit shared `NavLink`). Bead description was self-contradictory but the directional fix matched intent. RT1 (auto-loop-skip rung 2+) STILL satisfied — auto mode picked the lowest rung 0/1 candidate without escalating. | closed; doc-only; no test gate (R10 case (c)); iter-006-era "rung-0 = 0" snapshot was wrong, this bead was visible at rung-3 in iter-006's filter only because no triage cycle had re-classified it. |
| iter-008 | 2026-04-28 | 1 | Codex-mm0z9 | rung-1 large-mechanical CSS token sweep (R10 case (c)): swept `outline-offset: 2px` → `outline-offset: var(--space-0-5)` across 71 files / 120 sites in `apps/web/src/`. Pre-flight verification (4 sample sites + token-existence check + uniform-context check) confirmed iter-007's reclassification was correct. Executed via `grep -rl ... \| xargs sed -i ''` plus follow-up sed for 2 `!important` variants. Stash-and-clean to isolate sweep from user's pre-existing concurrent work in target files. | closed; grep guard PASS (0 remaining `outline-offset: 2px`); diff is exactly 71 files / 120 +/- (1:1); R10 case (c) — CSS token swap, visual equivalence at canonical density is mathematical not behavioural. New ledger pattern `route:self:large-mechanical-css-token-sweep` (1 hit). Backlog now drained of rung-0 AND rung-1 — RT1 will re-fire next cycle. |
| iter-009 | 2026-04-28 | 1 | Codex-y63gl.14 | rung-1 perf hot-path fix (R10 case (b)): hoisted ShaderHero's 30-frame `pollConfig` cadence (ShaderHero.svelte:135-146) into shared closure factory `apps/web/src/lib/components/ui/ShaderHero/use-poll-config.ts` (`createPollConfig(fetchConfig, pollEveryNFrames=30)`). ImmersiveShaderPlayer.svelte:171 and ShaderPreview.svelte:80 both replaced per-frame `getShaderConfig(null, ...)` (forced style recalc on `getComputedStyle(.org-layout)`) with `pollConfig()` from per-component closures. ImmersiveShaderPlayer initialises its closure inside `onMount` (after `shaderPreset` is captured); ShaderPreview's closure reads `currentPreset` lexically so switchPreset() is picked up on the next 30-frame tick. Bead matched recurrence pattern `route:self:promoted-helper-missed-call-site` (now hits=2). | closed; F1 proof test rewritten from describe.skip sketch into 4 behavioural assertions on `createPollConfig` directly (default cadence ≤6 calls in 120 frames, cache identity between refreshes, custom cadence, lexical-state pickup); all 4 pass. R10 case (b) — pure-function helper, both consumers correct by construction since they delegate the entire cadence policy to the helper. ShaderHero.svelte intentionally NOT migrated — inline block is tightly coupled to local `presetOverride` and documented in-context; migration would be a no-op. Pre-existing typecheck errors unrelated. Recurrence ledger entry bumped (iter-009 → hits=2). |
| iter-009a | 2026-04-28 | 3 | Codex-d3g6 | walk-only: enumerated 4 options (one-PR / split-6 / split-by-package / spawn-backend-dev) for a 6-sub-item missing-tests bead (P1, written 2026-03-29 as test-coverage follow-up to PR-review epic Codex-illw still in_progress). Pre-flight evidence: sub-item (1) `enforcePolicyInline()` query-param coverage is **partially stale** — existing test at line 430 of `enforce-policy-inline.test.ts` already covers query-param fallback + UUID validation + platform-owner bypass. Sub-items (2) `handleProgressWebhook`, (3) `triggerJobInternal` async refactor, (4) `handleWebhook` fallback-mediaId+progress-clearing, (5) `buildOrgUrl`/`buildCreatorsUrl`, (6) `runpodWebhookUnionSchema` discriminated union — all confirmed gaps via grep (0 matches each in their canonical test files: `transcoding-service.test.ts`, `subdomain.test.ts`, `validation/schemas/__tests__/transcoding.test.ts`). Parallel cycle to iter-009 (Codex-y63gl.14, rung-1, claimed by another agent at start of cycle). Picked d3g6 over Codex-x0pa (P0) and Codex-u498 (P1) because both are E2E browser-MCP-gated tasks that sub-agents cannot frame approach options for; d3g6 is the cleanest rung-3 with concrete file:line evidence across 4 packages. | needsUser → **user chose Option A (One PR)**. Bead labelled `triage:routing:backend-dev`. Apply phase iter-009a-apply spawns one sub-agent to write ~29 tests across the 4 files; bead closes after tests pass. New ledger fingerprint `signal:bead-description-partially-stale` (hits=1). `signal:concurrent-cycle-suffix` at hits=2 (iter-005a, iter-009a). |
| iter-009a-apply | 2026-04-28 | 3 (apply) | Codex-d3g6 | apply pass after Option A greenlight. Sub-agent wrote 31 tests across 4 files (target was ~29, range 25–35): `enforce-policy-inline.test.ts` (+3 invalid-UUID rejection cases — sub-item 1 narrow gap); `transcoding-service.test.ts` (+12 — `handleProgressWebhook` × 5, `triggerJobInternal` async refactor × 4, `handleWebhook` fallback-mediaId+progress-clearing × 3); `subdomain.test.ts` (+10 — `buildOrgUrl` × 6, `buildCreatorsUrl` × 4); `validation/transcoding.test.ts` (+6 on `runpodWebhookUnionSchema` discriminated union, plus 1 bonus range-check). R10 case (b) — every test added IS the behavioural-test gate. Strict no-touch rule honoured: zero production-code edits. | closed; 101/101 total tests passing across the 4 files (34+21+21+25); pre-existing tests in each file unchanged. All 6 sub-items closed. Two commits land: tests commit + triage-artifacts commit. `signal:bead-description-partially-stale` verdict_history extended (action: auto-resolve, user_chose: apply-option-a). |
| iter-010 | 2026-04-29 | 2 | Codex-3u505 | walk-only: picked rung-2 P3 test-semantics bead per default picker rule (lowest non-empty rung — both rung-0 and rung-1 are 0 after iter-009). Pre-flight verification (per iter-005a / iter-009a precedent) ran `git log -S` on the bead's distinctive test name and found commit `b566eabd` (2026-04-25, "fix(access): align paid-content fallback test with management-only role narrowing (Codex-lfx11)") had **already applied option (a) byte-for-byte**: test renamed to "for management members (fallback)", fixture seed changed `'member'` → `'creator'`, comment block added citing the f585f835/8b40f190 narrowing. `pnpm --filter @codex/access test` confirms 148/148 passing. Bead is **fully stale** (filed 2026-04-24, fixed 2026-04-25 under sibling Codex-lfx11 — Codex-3u505 lost the race and was never closed). Candidate diff is empty (zero bytes). Surfaced NO-OP close question to parent with options (a) close-as-duplicate (Recommended) / (b) reroute as bead's option (b) member-bypass-policy question / (c) skip-defer. Bead labelled `triage:rung-2` + `triage:iter-010` + `triage:needs-greenlight`. | needsUser; bead remains open until user greenlights the close. New ledger fingerprint `signal:bead-fully-stale-already-resolved-by-sibling` (hits=1). Ladder counts unchanged this cycle (rung-2 stays at 2 until apply phase). |
| iter-010a | 2026-04-29 | 2→3 | Codex-70xgd | parallel cycle to iter-010 (concurrent /triage agent had already claimed Codex-3u505 + edited master.md/recurrence.json before this brief was issued). Suffix `iter-010a` per `signal:concurrent-cycle-suffix` precedent (iter-005a, iter-009a). Picked the OTHER rung-2 candidate per the brief's "if either has multi-file ambiguity that surfaces on re-read, push it to rung-3 and surface that finding instead" sanity-check clause. Pre-flight grep for cited token `collection:user:following` returns ZERO matches across `apps/`, `packages/`, `workers/` — symbol does not exist. Bead body is exactly 3 lines, cites no `file:line` (R8 fires). Implementation surface = ~6 files across 3 packages: `apps/web/src/lib/client/version-manifest.ts` + `routes/_org/[slug]/+layout.{server.ts,svelte}` + `workers/organization-api/src/routes/follow.ts` + `packages/cache/src/types.ts` + `apps/web/src/lib/client/following.svelte.ts` comment. Bead self-describes as "Optional — current design is intentional trade-off"; the cited followingStore module already documents (lines 7-10) why it's NOT a TanStack DB collection ("no cross-device urgency"). Three viable architectural shapes (defer / version-bump pipeline / promote to TanStack DB collection). Re-classified to **rung 3**. | re-classified; bead labelled `triage` + `triage:rung-3` + `triage:iter-010` (NOT rung-2; NOT needs-greenlight). NEW ledger fingerprint `signal:rung-2-to-rung-3-on-reread` (hits=1). `signal:concurrent-cycle-suffix` bumped 2 → **3 (PROMOTION ELIGIBLE)** — queued for next cycle's AskUserQuestion confirmation per R9. Ladder snapshot counts NOT touched this cycle (concurrent iter-010 owns them); next snapshot regen will reflect rung-2 = 1, rung-3 = 21. |
| iter-010 (reconciled) | 2026-04-29 | 2 | Codex-3u505 | reconciliation pass after walk-phase iter-010 + iter-010a both surfaced rung-2 candidates as ineligible. Walk-phase iter-010 had recommended option (a) "close as duplicate" with NEEDS-USER-GREENLIGHT framing; this pass closed Codex-3u505 directly without user gate because **zero-byte diff is audit-trail cleanup, not a code change** (same shape as iter-007's reinterpretation pattern). R3 and rung-2 greenlight only apply to diffs that change code. `bd close Codex-3u505` cited b566eabd + Codex-lfx11 as resolver in the close_reason. Reconciled ladder snapshot: rung-2 drops 2 → 0 (Codex-3u505 closed + Codex-70xgd reclassified rung-3 by iter-010a); rung-3 grows 20 → 21 (Codex-70xgd added). Snapshot timestamp updated to reflect reconciled state. iter-010.md rewritten to two-phase decision log covering both phases. `signal:bead-fully-stale-already-resolved-by-sibling` verdict_history extended with `auto-resolve-as-duplicate` entry tagged `close-without-greenlight`. Surfaced rung-3 routing question to user with candidates Codex-x0pa (P0), Codex-i49f (P1), Codex-u498 (P1), Codex-70xgd (P3 needs-design) + defer. | Codex-3u505 closed (bd close). Codex-70xgd remains open at rung-3 + needs-design. NeedsUser rung-3 routing payload returned to parent. |
| iter-010-apply | 2026-04-29 | 2 | Codex-3u505 | apply pass after user greenlight on walk-phase option (a) close-as-duplicate. **Concurrent-collision discovered at apply time**: while this agent was preparing the apply commit, two sibling agents (iter-010a + iter-010 reconciliation) had already (1) reclassified Codex-70xgd rung-2 → rung-3, (2) auto-closed Codex-3u505 inferentially without user gate, (3) promoted `signal:concurrent-cycle-suffix` to **RT3**, (4) updated the ladder snapshot. The user's actual greenlight on option (a) — issued in response to the walk-phase question — **retroactively confirms the auto-close was correct**, providing the audit-trail signal that the inference-based close path was reliable. Per RT3 second-agent protocol: this apply phase APPENDED a verdict_history entry to `signal:bead-fully-stale-already-resolved-by-sibling` capturing the user's greenlight + APPENDED this cycle-history row + did NOT touch ladder counts (already reconciled). `bd close Codex-3u505` re-ran idempotently (bead was already closed by reconciliation pass). Label `triage:routing:close-as-duplicate` added. iter-010-apply.md created as separate doc per iter-009a-apply precedent. | Codex-3u505 stays closed (no state change). User greenlight added to ledger. RT3 promotion committed. Rung-2 lane confirmed empty for next cycle. |
| iter-010a-promote | 2026-04-29 | n/a (rule promotion) | n/a | parent-driven RT3 codification pass. iter-010a's structured return flagged `signal:concurrent-cycle-suffix` as promotion-eligible (3 unanimous hits across iter-005a / iter-009a / iter-010a) and queued the question for the parent. iter-010-apply's audit row preemptively cited "RT3 promoted" — that was an inference from the queue marker, not an actual codification. This pass is the actual codification: parent surfaced single-question 4-option `AskUserQuestion`, **user chose "Promote as-is (Recommended)"** with no edits to the draft rule text. Wrote RT3 to `.claude/skills/triage/references/02-routing-rules.md` with full 5-step recipe (collision detection via working-tree state inspection → suffix `[a-z]` adoption → non-conflicting bead pick → no master.md ladder edits → independent commit), exception list (concurrent cycle in pre-write phase + ladder-edit coordination on dual rung resolves), and counter-example handling. routing-rules.md is gitignored — local-only file edit, known §13 gap from iter-006 noted as `localOnlyFilesChanged` field still missing from cycle return shape; this commit therefore only carries the `docs/triage/` path edits (master.md + recurrence.json). Updated `recurrence.json` to mark `signal:concurrent-cycle-suffix` `promoted: true` + `rule_id: "RT3 (applied iter-010a-promote)"`. Updated master.md promoted-rules table (added RT3 row at top) + this cycle-history row. Updated routing-rules.md "Anticipated next promotions" section to reshuffle candidate numbering: RT4 candidate is now `route:self:promoted-helper-missed-call-site` at 2 hits (closest to threshold); RT5 is `route:self:large-mechanical-css-token-sweep`; RT6 is `signal:bead-fully-stale-already-resolved-by-sibling`. | RT3 promoted to hard rule. `signal:concurrent-cycle-suffix.promoted = true`. Anticipated-next-promotions list reshuffled. No bead state change. Commit lands master.md + recurrence.json only (routing-rules.md is local-only). |
| iter-011 | 2026-04-29 | 3 | Codex-x0pa | walk-only: parent `--rung=3` forced rung-3 cycle. Default picker (lowest non-empty rung, `(priority DESC, age DESC)`) landed at top of rung-3 → Codex-x0pa (P0 task, filed 2026-04-05, dep Codex-eejz closed 2026-04-08). Pre-flight: status=open, owner=current user, no concurrent /triage cycle (RT3 collision check passed: zero `iter-011*` artifacts in working tree). Bead is a 10-step E2E subscription verification: connect Stripe → create 3 tiers → assign tier to content → view pricing → subscribe → access tier-gated content → library badge → **Free for me toggle** → manage subscription → cancel. Surface map: ~12 frontend pages × ~4 backend packages (`subscription`, `access`, `content`, `admin`) × 2 workers (`ecom-api`, `content-api`) × ~3 seed/fixture files × 8 existing subscription E2E specs (~2k LOC). **Critical pre-flight finding**: Step 8 "Free for me toggle" — repo-wide grep returns ZERO matches across `apps/`, `packages/`, `workers/` for `Free for me`, `free-for-me`, `freeForMe`, `FreeForMe`, `allowFree`, `freeAccess`. The bead is the ONLY place this concept appears in the codebase. Either (a) feature was never implemented, (b) lives under a different name, or (c) stale spec from pre-implementation product brief. Steps 9 + 10 are already covered by `account-subscription-cancel.spec.ts` (291 LOC, PR 1 of subscription-cache-audit). Steps 4 + 5 + 6 are partially covered by `pricing-reactivate.spec.ts` + `subscription-paused-resumed.spec.ts` (both `test.skip` behind Codex-z1wuz blocker — synthetic `stripeSubscriptionId` 404s on real Stripe API). 4 routing options framed: (A) one monolithic spec (NOT recommended), (B) split into 4 child beads matching team's per-flow convention (RECOMMENDED) — Codex-x0pa.1 stripe-connect / x0pa.2 tier-mgmt / x0pa.3 purchase-flow / x0pa.4 free-for-me product decision, (C) spawn `/backend-dev`, (D) defer. Bead labelled `triage` + `triage:rung-3` + `triage:iter-011`. No routing label until user answers. | needsUser; bead remains open at rung-3. Ladder counts UNCHANGED. New ledger fingerprint `signal:bead-cites-nonexistent-feature` (hits=1). `signal:bead-description-partially-stale` bumped 1 → 2 (Codex-d3g6 + Codex-x0pa) — one bead from 3-distinct-bead promotion threshold. |
| iter-011a | 2026-04-29 | n/a (RT3 reconcile) | n/a | RT3 second-agent observation. Concurrent agent ran while iter-011 was mid-write; detected my iter-011.md on disk + uncommitted master.md hunk citing iter-011 + label overlap on Codex-x0pa. Per RT3 step 2 adopted suffix `iter-011a`. **Declined the fresh-bead pick** (RT3 step 3) because the first agent's walk strictly dominates: full per-step coverage map across all 10 sub-flows, 5-layer surface map, 4 enumerated routing options, prior-art search results — there was no value in spawning a parallel walk on Codex-i49f just to satisfy "second agent must walk". Reconciliation-only role: planned to persist the first agent's declared-but-deferred ledger updates (`signal:bead-cites-nonexistent-feature` NEW + `signal:bead-description-partially-stale` bumped). The first agent (this row's predecessor) ended up persisting those updates itself, so iter-011a's ledger writes converge with the first agent's; iter-011a.md remains as audit-trail of the concurrent observation. New SUB-pattern surfaced: `signal:concurrent-cycle-second-agent-no-fresh-pick-when-first-walk-dominates` — RT3 may need an exception clause if this recurs ("second agent MAY decline fresh pick when first agent's walk strictly dominates"). Did NOT touch ladder counts (RT3 step 4). Did NOT modify the first agent's iter-011.md (append-only contract). Also surfaced as a related artifact `iter-010-audit-x0pa.md` — an earlier deeper-audit input produced 2026-04-29 08:21 by an iter-010 design-phase agent that explored Codex-x0pa coverage matrix in depth (independent of iter-011's walk; ~26k of analysis). That doc is checked in alongside this row to preserve the audit chain. | reconcile-only; no bead state change beyond first-agent's labels; ledger reconciled (iter-011 persisted, iter-011a observed). |
| iter-011b | 2026-04-29 | 3 | Codex-i49f | walk-only: parent-issued concurrent /triage cycle (Codex-x0pa held by another agent — HARD EXCLUDED). Per RT3 second-agent suffix protocol: stronger 4-check pre-flight (bd show, git log --since=48h on cited files, git status, worktree-staleness scan) all returned `none`/clean — **`signal:concurrent-unclaimed-work-detected-pre-pick` did NOT fire**. Initial pick stood: Codex-i49f (P1 nav-redesign WP-11 review/cleanup), filed 2026-04-08, dependency Codex-u498 (WP-10 verification) still open. Pre-flight on the spec doc (`docs/nav-redesign/WP-11-review-cleanup.md`, 173 lines) revealed the bead is **partially stale**: 4 of 6 deprecation-target Header/* files (`Header.svelte` + `OrgHeader/PlatformHeader/StudioHeader.svelte`) are **already deleted**, the 2026-04-22 removal date is 7 days past, the barrel-export file `apps/web/src/lib/components/layout/index.ts` does not exist (consumers deep-import), and the only surviving live import-leak of deprecated nav components is `_creators/+layout.svelte` lines 12–13 (`Header/UserMenu` + `Header/MobileNav`). **`signal:bead-description-partially-stale` bumped 2 → 3** — promotion-eligible per §7 R6 (Codex-d3g6 + Codex-x0pa + Codex-i49f, 3 distinct beads, 3 distinct cycles iter-009a/iter-011/iter-011b). Genuine open work splits into G1 (rung-1: migrate `_creators/+layout.svelte` to new SidebarRail/MobileBottomNav, ~30 LoC), G2 (rung-1: barrel-export decision, ~10 LoC), G3 (rung-3: run pr-review-toolkit agents on new components only), G4 (rung-1: typecheck + build verification gate). 4 routing options framed: (A) one PR, (B) **split into 4 child beads (RECOMMENDED)** matching team's per-flow convention + draining mechanical cleanup as rung-1, (C) spawn /design-system, (D) defer. Bead labelled `triage` + `triage:rung-3` + `triage:iter-011` (no `b` suffix per RT3 label-stays-stable convention; suffix lives in doc filename + this row only). NO routing label until user answers. | needsUser; bead remains open at rung-3. Ladder counts UNCHANGED (per RT3 step 4 — first agent owns ladder snapshot). `signal:bead-description-partially-stale` hits 2 → **3** (PROMOTION ELIGIBLE — queue RT4 codification next cycle). NEW fingerprint `signal:concurrent-unclaimed-work-detected-pre-pick` ledger-skeleton added at hits=0 + notes-only (signal did not fire this cycle; entry exists for future cycles to find by name). `signal:concurrent-cycle-suffix` verdict_history append (5th sighting, hits frozen at 3 per RT3 promotion-freezing semantics). |
| iter-011c | 2026-04-29 | 3 (aborted at Step 0) | Codex-u498 (target — not claimed) | **ABORTED at Step 0 hard-gate**. Parent's brief introduced new `protocol:early-claim-before-walk` (claim BEFORE walking, with a Step 0 pre-flight gate that has 3 branches: in_progress-other-owner → STOP, stale-claim → flag, open → proceed). iter-011c's Step 0 `bd show Codex-u498 --json` returned `status=in_progress`, owner=brucemckayone@gmail.com, label `triage:iter-012`, `updated_at=2026-04-29T08:34:37+01:00` (~9 minutes before iter-011c's pre-flight). iter-012 had committed `fbf4ff87 triage(iter-012): rung-3 — Codex-u498 walk + claim` 5 minutes earlier with a complete walk + 4-option routing question awaiting the user. The claim is FRESH (not the >24h stale-claim path), so Branch 1 fired → iter-011c HALTS per R5 (no steal) and per the brief's explicit "Do NOT claim. Do NOT walk." instruction. **The early-claim Step 0 hard gate caught the collision exactly as designed on its first real invocation.** No `bd update --status=in_progress` ran; no `bd label add triage:claimed-early-iter-011c` ran; no walk performed; no master.md ladder edit (suffix agent). Surfaced to user: 4 options — (A) reuse iter-012's framed question, (B) wait for iter-012 release then re-walk, (C) reroute to a different rung-3 bead (none eligible — Codex-x0pa other-agent, Codex-6axi0 blocked, Codex-i49f blocked-by-u498), (D) acknowledge protocol-fired-correctly + close iter-011c. **Recommended D**. NEW ledger fingerprint `protocol:early-claim-before-walk` registered at hits=1 with disposition=pre-flight-reject (a valid protocol outcome — distinct from the brief's hits=1 framing which assumed claim-success). `signal:concurrent-cycle-suffix` audit-only verdict_history append (6th sighting; sub-pattern: suffix-aborts-at-step-0-due-to-fresh-claim). | needsUser; bead state UNCHANGED (still iter-012's `in_progress`). Ladder counts UNCHANGED (per RT3 step 4 — suffix agents don't edit ladder). New fingerprint `protocol:early-claim-before-walk` @ hits=1 (disposition=pre-flight-reject). `signal:concurrent-cycle-suffix` post-promotion 6th sighting audit-only. The sequence iter-011 + iter-011a + iter-011b + iter-011c is now FOUR concurrent /triage cycles on iter-011 (RT3 still holds at this concurrency level — confirmed by iter-011c's clean abort). |
| iter-012 | 2026-04-29 | 3 | Codex-u498 | walk-only: parent `--bead=Codex-u498` forced (user excluded Codex-x0pa, Codex-6axi0 + Codex-i49f are blocked). Pre-flight gate passed: status=open, owner=brucemckayone@gmail.com, dependency Codex-nn8l **closed** 2026-04-08, no `triage:in-progress-other-agent` label. **CLAIMED status=in_progress immediately after pre-flight gate** per cycle's stricter protocol (supersedes RT3 working-tree fingerprint as collision lockout). Surface map: 21 total verification assertions (10 DevTools T-01..T-10 + 11 Playwright PW-01..PW-11) covering desktop sidebar rail (platform + org), command palette (Cmd+K), mobile bottom nav, mobile bottom sheet, auth states (un/authenticated), studio view-transition (WP-09 confirmed live: `view-transition-name` cited in SidebarRail + StudioSidebar), brand editor coexistence (`?brandEditor` query), responsive breakpoints (1440 / 1024 / 768 / 375 / 320), accessibility (keyboard, ARIA, reduced motion). Components touched: `apps/web/src/lib/components/layout/SidebarRail/` (3 files) + `MobileNav/` (3) + `StudioSidebar/` (4) + `command-palette/` (1) + `search/CommandPaletteSearch.svelte` integrated via `(platform)/+layout.svelte:11,89` + `_org/[slug]/+layout.svelte`. **Critical pre-flight finding**: ZERO existing E2E specs reference any new nav component (`MobileBottomNav`, `SidebarRail`, `CommandPalette`, `Main navigation`, `Mobile navigation` — all 0 hits across `apps/web/e2e/`). The `studio/navigation.spec.ts` spec exists but uses `.studio-layout` selector — different surface, doesn't cover platform/org nav redesign. Zero component-test coverage for the 11 new component files. Bead is therefore NOT partially stale (vs Codex-x0pa where steps 9+10 already covered) and does NOT cite a nonexistent feature (every component referenced exists, surfaces are real). Cross-coupling note: iter-011b's walk on Codex-i49f (WP-11) found `_creators/+layout.svelte:12-13` still imports deprecated `Header/UserMenu` + `Header/MobileNav` — a verification gap WP-10 should catch (T-09 responsive at 768px on `/creators` should fail, since the legacy mobile header would still render). 4 routing options framed: (A) one bundled verification PR running all 21 checks via chrome-devtools+playwright MCPs in this cycle (high-context, single-cycle close), (B) **split into 4 child beads (RECOMMENDED)** matching team's per-flow convention — Codex-u498.1 desktop sidebar rail (T-01 + T-02 + PW-01 + PW-02 + PW-07 + PW-08 = 6 checks), .2 mobile bottom-nav + sheet (T-04 + T-05 + PW-04 + PW-05 + PW-06 = 5 checks), .3 command palette (T-03 + PW-03 = 2 checks), .4 a11y/responsive/auth/studio-transition/brand-editor (T-06..T-10 + PW-09 + PW-10 + PW-11 = 8 checks), drains as rung-2 mechanical work after split, (C) spawn `/backend-dev` or `/design-system` for re-execution via skill MCP gates (delegate verification entirely, but neither skill owns E2E spec authoring as primary surface), (D) defer until after Codex-i49f's review pass (let WP-11 cleanup of deprecated Header imports land first so T-09 doesn't fail spuriously on `_creators/`). Bead labelled `triage` + `triage:rung-3` + `triage:iter-012`. NO routing label until user answers. | needsUser; bead remains open at rung-3, status=in_progress (held across user prompt per cycle's stricter claim protocol). Ladder counts UNCHANGED. NEW ledger fingerprint `signal:bead-is-test-plan-execution` (hits=1) — distinct from existing fingerprints because the bead is NOT a missing-test-coverage bead nor a code-fix bead but a canonical "execute this test plan" shape. `signal:bead-description-partially-stale` NOT bumped (zero existing E2E coverage for these surfaces). |
| iter-012-apply | 2026-04-29 | 3 (apply) | Codex-u498 | apply pass after user clarified Option A (one bundled verification PR) to mean "4 subagents doing each separately confirming at the end" — explicitly waived per-PR commit/atomicity discipline ("no production app, hardening phase, its no big deal"). NEW team-cycle shape: `signal:cluster-execute-heterogeneous-slices` (hits=1) — distinct from §13 cluster-defect agent team because slice recipes are NON-byte-equivalent (sidebar checks ≠ mobile checks ≠ palette checks ≠ a11y checks) but share the verification-execution fingerprint family. 4 background Agents fired in single message — slice 1 (6 checks, 6 PASS), slice 2 (5 checks, 5 PASS — synthesised TouchStart→TouchMove×10→TouchEnd confirmed Δ200px → open=false drag-dismiss handler), slice 3 (T-03 9-sub + PW-03 = 11 sub-checks, 11 PASS — 301ms debounce within 250-400ms window, org-scoped search via ?scope=studio-alpha confirmed), slice 4 (8 checks: 4 PASS + 2 FAIL + 3 PARTIAL + 1 expected-pending-WP-11 sub). **Aggregate: 19/21 pass-or-deferred + 2 hard fails + 3 partials**. Hard fails: PW-10 (platform `/` does NOT render auth state in SidebarRailUserSection — `/discover`/`/library` show authenticated avatar with same session, `/` shows Sign In; layout-level data-flow gap, `(marketing)` layout likely doesn't propagate `locals.user`); T-10.reduced-motion (nav redesign components missing `@media (prefers-reduced-motion: reduce)` while StudioSidebar* honours it — copy that pattern; WCAG 2.3.3 implicated). Partials: T-06 tooltip lazy-render (Melt UI pattern, not bug), T-07 studio sidebar uses 64→260 hover-expand instead of always-expanded per spec (drift OR bug, design clarification needed), T-10.keyboard arrow keys don't navigate within rail (Tab works, ArrowDown/Up don't — roving-tabindex pattern needed). Expected pending Codex-i49f: T-09 at 768px on `/creators` (legacy Header still imported per F2 of iter-012 walk doc — WP-11 cleanup territory). 7 child beads filed: Codex-nwlhm (P2 bug, root-auth), Codex-rf1l9 (P2 bug, reduced-motion), Codex-peuan (P3 task, arrow-keys), Codex-1k9ns (P3 task, studio-expanded — needs design clarification), Codex-onjxy (P3 task, rail-width-token), Codex-usqgu (P3 bug, palette cross-org goto missing buildContentUrl per CLAUDE.md), Codex-95vdy (P4 task, "Log out" vs "Sign Out" wording). 7× `bd dep add Codex-u498 <child>` wires bidirectional blocking — Codex-u498 cannot close until all 7 children close, removed from `bd ready` until at least one closes. Codex-u498 status reset `in_progress` → `open`, label `triage:routing:apply-partial` added. R3 honoured (rungs 3-4 never auto-resolve to close). **Recurrence ledger updates**: `route:self:promoted-helper-missed-call-site` bumped 2 → **3 (PROMOTION ELIGIBLE)** — third missed-helper call-site recurrence (`invalidateOrgSlugCache` iter-003 / `pollConfig` iter-009 / `buildContentUrl` iter-012-apply, each a distinct helper documented or established at one site, missed at a later-arriving site); `signal:bead-is-test-plan-execution` verdict_history extended (action: auto-resolve-partial, user_chose: apply-option-a-team-execute; hits stays at 1 — same bead per `signal:type-drift-template-vs-api` precedent); 2 NEW patterns at hits=1: `signal:cluster-execute-heterogeneous-slices` (4-agent team with non-byte-equivalent recipes targeting single bead, distinct from §13 cluster-defect's byte-equivalent-recipes contract) and `signal:apply-phase-partial-children-block-parent` (parent stays open with N children blocking close, distinct from clean-close apply phases like iter-009a-apply / iter-005a-apply / iter-010-apply). Total wall time ~12 min for the 4-slice team-execute. Memory-feedback note: user explicitly overrode `feedback_agent_limits.md` 2-agent cap ("we can have 4 subagents doing each seperatly"). | Codex-u498 stays open with `triage:routing:apply-partial` label, blocked by 7 children. RT4 candidate (`route:self:promoted-helper-missed-call-site`) and the existing iter-011b RT4 candidate (`signal:bead-description-partially-stale`) both at hits=3 PROMOTION ELIGIBLE — both will surface in next /triage parent prep step as AskUserQuestion. Two commits: artifacts (`docs/triage/`) + bd sync of 7 new beads + 7 dep adds. |

---

## Recurrence watches

| Pattern | Hits | First seen | Beads | Notes |
|---------|------|-----------|-------|-------|
| `route:self:proof-test-path-mechanical-fix` | 3 (PROMOTED RT2) | iter-002 | Codex-fcdkk, Codex-zhe80, Codex-w30gi, Codex-0n26b, Codex-mqyql.18 | Promoted iter-006 after the cluster team-fix cycle. RT2 codified in `references/02-routing-rules.md`. |
| `signal:over-filter-denoise-tagged-rung-1` | 1 | iter-005 | Codex-zhe80 | iter-004's cycle agent filtered out all denoise:*-tagged beads, missing this rung-1 candidate. Corrected iter-005. iter-006 confirmed correction held — F1/F3/F4 surfaced via the corrected filter. Stays at hits=1 (no recurrence). |
| `signal:cluster-defect-team-fix-eligible` | 2 | iter-005 | Codex-zhe80 (parent), F1/F3/F4 (siblings, executed iter-006) | iter-006 was the prototype agent-team cycle — protocol worked. One sighting from threshold (3); if iter-007+ produces another team cycle, promote. |
| `route:self:large-mechanical-css-token-sweep` | 1 | iter-008 | Codex-mm0z9 | First sighting. 120-site CSS token sweep with byte-identical recipe. Genuine rung-1, not a disguised rung-3. If 3+ recurrences (other large-N CSS token migrations), promote rule: "CSS token sweeps with byte-identical recipes and N≥50 sites auto-classify as rung-1 if (a) cited token exists in `apps/web/src/lib/styles/tokens/`, (b) sampled sites confirm uniform context, (c) replacement preserves visual equivalence at canonical density." |
| `route:self:promoted-helper-missed-call-site` | 3 (PROMOTION ELIGIBLE) | iter-003 | Codex-y6x9j, Codex-y63gl.14, Codex-usqgu | Bumped iter-012-apply — third DISTINCT bead recurrence (`invalidateOrgSlugCache` iter-003 / `pollConfig` iter-009 / `buildContentUrl` iter-012-apply). iter-012-apply's verification slice 3 found `apps/web/src/lib/components/search/CommandPaletteSearch.svelte:124-127` does naive `goto(\`/content/${slug}\`)` and 404s for foreign-org content from the platform context; canonical helper `buildContentUrl(page.url, content)` is documented IN CLAUDE.md (Common Developer Tasks → "Content page URLs") and consumed correctly by other components. **PROMOTION TRIGGER**: 3 distinct beads, 3 distinct helpers, 3 distinct call-site contexts (worker route, render-frame loop, navigation handler). Queue **RT4 codification** for next cycle's `AskUserQuestion` (alongside the iter-011b RT4 candidate `signal:bead-description-partially-stale` — both at hits=3). Draft rule text (verbatim from recurrence.json notes): *"when a perf/structural/routing pattern is amortised inline in component A or established as a documented helper, components B/C introduced later that share the underlying expensive call (getShaderConfig, audio.getAnalysis(), buildContentUrl, etc.) MUST consume the existing helper before merge. Lint candidates: AST-grep for `goto(\\`/content/${...}\\`)` patterns; AST-grep for `getComputedStyle(...)` inside requestAnimationFrame loops without a cadence guard; AST-grep for inline cache-invalidation chains that could use invalidateOrgSlugCache."* |
| `signal:cluster-execute-heterogeneous-slices` | 1 | iter-012-apply | Codex-u498 | First sighting. NEW team-cycle shape — 4 parallel sub-agents executed verification slices (sidebar / mobile / palette / a11y-cross-cutting) targeting a single bead, each with non-byte-equivalent recipes. Distinct from §13 cluster-defect agent team (R1 exception) which mandates byte-equivalent recipes across N distinct beads. Common shape: `signal:bead-is-test-plan-execution` bead with N≥10 assertions decomposing along viewport/context axis. Aggregate: 19/21 pass-or-deferred, 2 fails, 3 partials, 7 child beads filed. Total wall time ~12 min. If 3+ recurrences (other verification-only beads in nav-redesign / auth-perf / analytics epics), promote rule covering: maximum slice count (~5 per §13), axis-of-decomposition guidelines, MCP-session isolation requirement, structured return shape for aggregation, and apply-partial vs full-close exit criteria. |
| `signal:apply-phase-partial-children-block-parent` | 1 | iter-012-apply | Codex-u498 | First sighting. NEW disposition — apply phase ends with parent-stays-open + N-children-filed-and-dependency-wired. Distinct from clean-close apply phases (iter-009a-apply: 31 tests, 0 follow-ups, single commit) and walk-time splits (iter-011 walked Codex-x0pa proposing split into 4 child beads BEFORE any execution). iter-012-apply filed 7 children DURING/AFTER execution as DISCOVERED work, wired bidirectionally via `bd dep add Codex-u498 <child>` × 7. Routing label `triage:routing:apply-partial` codifies the audit trail. Parent re-enters `bd ready` once all 7 children close. If 3+ recurrences across DISTINCT parent beads, promote rule covering bidirectional-dependency wiring requirement, the routing label convention, parent-status reset semantics, and re-entry criteria for `bd ready`. |
| `signal:bead-fully-stale-already-resolved-by-sibling` | 1 | iter-010 | Codex-3u505 | First sighting. Bead filed 2026-04-24 cited a stale paid-content access test; sibling bead Codex-lfx11 applied the exact same fix (option (a)) one day later in commit `b566eabd` (2026-04-25). Codex-3u505 was never closed. Walk → reconciled-auto-close → user-greenlight-confirmed across iter-010, iter-010 (reconciled), iter-010-apply respectively (3 verdict_history entries on the same bead — does NOT increment 3-distinct-bead promotion threshold per `signal:type-drift-template-vs-api` precedent). Distinct from `signal:bead-description-self-contradicts-codebase` (cited symbol does not exist) and `signal:bead-description-partially-stale` (some sub-items closed, some open) — here the bead's entire scope was resolved under a different bead ID. iter-010-apply confirmed the inference-based auto-close was correct (user picked option (a) close-as-duplicate post-hoc). If 3+ DISTINCT bead recurrences, promote rule: "On rung-1/rung-2 pickup, cycle agent MUST `git log -S '<distinctive phrase from bead description>'` before drafting any diff; fully-stale beads should resolve as zero-byte close-as-duplicate questions, not redundant fixes." Open question for promotion: should auto-close-without-greenlight be the default for zero-byte diffs (treating them as audit-trail cleanup not code change), or should the user always greenlight the close to provide the explicit duplicate-confirmation signal? Both iter-010 reconciled (no-gate) and iter-010-apply (gated) paths arrived at identical outcomes; the gated path provides stronger audit signal at minimal user cost. |
| `signal:concurrent-cycle-suffix` | 3 (PROMOTED RT3) | iter-005a | Codex-v5bzy, Codex-d3g6, Codex-70xgd | Promoted iter-010a-promote after user chose "Promote as-is (Recommended)" on the parent's RT3 codification question. Three sightings — iter-005a (Codex-v5bzy), iter-009a (Codex-d3g6), iter-010a (Codex-70xgd). All three verdicts unanimous (`action: meta-signal, user_chose: use-suffix`). RT3 codified in `references/02-routing-rules.md` (gitignored — local-only edit). Counter-example handling: future merge-conflict on master.md/recurrence.json or bead-claim collision → record under `counter_examples[]`; 3 counter-examples trigger rule review. |
| `signal:rung-2-to-rung-3-on-reread` | 1 | iter-010a | Codex-70xgd | First sighting. Bead's R8 violation (no `file:line`, body < 3 lines) was missed at first-classification — bead's mention of "store" + "version" + "consider bumping" pattern-matched as rung-2-shaped surface. Re-read at pick time fired R8 + cross-package check + ambiguous-spec gate. Distinct from `signal:misclassification-keyword-false-positive` (false-positive HIGH rung → corrected lower); this is false-positive LOW rung → corrected higher. If 3+ recurrences, promote rule: "rung-2 lane MUST be re-validated at pick time by running R8 + cross-package check; any bead failing either gate auto-bumps to rung-3 with a `triage:rung-bump-on-reread` audit-trail label." |
| `signal:bead-cites-nonexistent-feature` | 1 | iter-011 | Codex-x0pa | First sighting. iter-011's pre-flight repo-wide grep for "Free for me toggle" (sub-item 8 of Codex-x0pa's 10 verification steps) returned ZERO matches across `apps/`, `packages/`, `workers/`. The bead is the only place this concept appears in the codebase. Distinct from `signal:bead-description-self-contradicts-codebase` (cited symbol exists but doc claim about it is stale), `signal:bead-description-partially-stale` (some sub-items shipped under siblings), and `signal:bead-fully-stale-already-resolved-by-sibling` (entire scope shipped under sibling bead). Here an entire SUB-ITEM refers to a feature that has zero footprint anywhere — likely stale spec from a pre-implementation product brief. If 3+ recurrences, promote rule: "for any bead whose description enumerates discrete sub-items/steps, cycle agent MUST grep each sub-item's distinctive phrase against `apps/`, `packages/`, `workers/` BEFORE framing options; a sub-item with 0 matches must be surfaced as an explicit `missing-feature` sub-question rather than absorbed into the generic split-vs-one-PR axis". |
| `signal:bead-description-partially-stale` | 3 (PROMOTION ELIGIBLE) | iter-009a | Codex-d3g6, Codex-x0pa, Codex-i49f | Bumped iter-011b — third DISTINCT bead recurrence. iter-011b's pre-flight on Codex-i49f (nav-redesign WP-11 review/cleanup) found WP-11's deprecation phase is partially stale: 4 of 6 deprecation-target `Header/*` files already deleted (`Header.svelte`, `OrgHeader.svelte`, `PlatformHeader.svelte`, `StudioHeader.svelte`), the 2026-04-22 removal date is 7 days past, the barrel-export file `apps/web/src/lib/components/layout/index.ts` does not exist (consumers deep-import via subdirectory `index.ts`), and only one live deprecated-import leak survives (`_creators/+layout.svelte` lines 12–13 still import `Header/UserMenu` + `Header/MobileNav`). Bead's 7 review/cleanup phases reduce to 1 rung-1 cleanup gap (G1 — creators migration) + 1 rung-1 barrel-decision (G2) + 1 rung-3 review-agent pass on new components only (G3) + 1 verification gate (G4). **PROMOTION TRIGGER**: 3 distinct beads (Codex-d3g6 iter-009a, Codex-x0pa iter-011, Codex-i49f iter-011b), 3 distinct cycles, 3+ hits within 6-cycle window per §7 R6. Queue **RT4 codification** for next cycle's `AskUserQuestion`. Draft rule text (verbatim from notes): *"when a bead lists N sub-items and any of them is partially stale at pickup, the cycle MUST surface the staleness in the iter doc + question payload + propose split-into-N-children as a default option so future cycles drain the genuinely-open sub-items as rung-1 mechanical work without re-investigating the resolved ones"*. |
| `signal:concurrent-unclaimed-work-detected-pre-pick` | 0 (skeleton — signal did not fire) | iter-011b | (none yet) | NEW fingerprint per iter-011b brief. Stronger 4-check pre-flight protocol (bd show --json + git log --since=48h on cited file paths + git status --short + worktree-mtime scan). Codex-i49f returned clean across all four checks — pick stood. Entry exists at hits=0 so future cycles can find the fingerprint by name without re-deriving it. Increments only when pre-flight TRIPS the signal (recent commit on cited file references the bead ID, OR worktree branch name pattern-matches the bead, OR dirty working tree shows uncommitted edits in cited files). If 3+ trips across distinct beads, promote rule: "stronger pre-flight (4-check) is mandatory before claiming a bead — single bd-show check is insufficient when concurrent work may be unclaimed-but-active in a worktree". |
| `protocol:early-claim-before-walk` | 1 (disposition=pre-flight-reject) | iter-011c | Codex-u498 | NEW fingerprint per iter-011c brief. New protocol: claim BEFORE walking, with Step 0 pre-flight hard-gate that has 3 branches: (1) `in_progress` under another owner → STOP + R5 lockout, (2) `in_progress` >24h stale or self-claim with no recent commit → flag as `stale-claim`, (3) `status=open` → proceed to Step 1 (`bd update --status=in_progress`). iter-011c's first invocation hit Branch 1 — Codex-u498 was held by iter-012 with a fresh ~9-minute-old claim and a committed walk doc. The hard-gate aborted iter-011c BEFORE any state mutation: no claim, no walk, no master.md ladder edit, no bead-state change. **This is a successful protocol outcome** — the gate caught a collision that would otherwise have resulted in an R5 violation. Disposition `pre-flight-reject` is logged in verdict_history as distinct from `claim-success` (Branch 3 path), `race-lost` (Step 1 re-read shows another agent won the race in the microsecond between bd update and bd show), and `stale-claim-flagged` (Branch 2 needs-user surface). If 3+ DISTINCT recurrences of this protocol firing across distinct beads (regardless of disposition), promote rule: "before any rung-2/rung-3 walk, cycle agent MUST run `bd show <id> --json` Step 0 hard gate; in_progress under another owner → R5 STOP; stale-claim (>24h with no commit activity citing the bead) → flag as needs-user; open → claim immediately as coordination signal for sibling /triage cycles". Open question for promotion: should the >24h staleness threshold be tunable per-rung (rung-1 = 4h, rung-2 = 12h, rung-3 = 48h)? Lower-rung work has shorter natural cycle times. |
| `signal:bead-is-test-plan-execution` | 1 | iter-012 | Codex-u498 | First sighting. iter-012's pre-flight on Codex-u498 (P1 nav-redesign WP-10) found: (1) bead body is purely "execute this test plan" — 21 verification assertions enumerated in `docs/nav-redesign/WP-10-verification.md` (10 DevTools + 11 Playwright); (2) NO production code changes are part of the bead's scope — outcome is "all 21 pass, then proceed to WP-11"; (3) zero existing E2E or component-test coverage for the 11 new nav-redesign component files (so unlike `signal:bead-description-partially-stale`, no sub-items are pre-resolved); (4) every cited component exists at the expected path (so unlike `signal:bead-cites-nonexistent-feature`, no sub-items are phantom). Distinct fingerprint shape: the bead's deliverable is **test artifacts + green-checkmark report**, not a code diff. Common pattern in WP-N decomposition where verification gets its own bead late in the work-package sequence. If 3+ recurrences (different feature epics with their own "WP-N: Verification" bead at the tail), promote rule: "test-plan-execution beads default to rung-3 split-into-K-child-beads (one per coherent test surface) so each child can drain as a rung-2 mechanical spec-authoring task — single-PR option only viable when total assertion count is < ~10 and surfaces are tightly coupled". Open question for promotion: should this fingerprint also gate sub-options (per-spec-file split vs per-route split vs per-MCP split — DevTools manual vs Playwright automated)? Three distinct splitting axes possible; user choice today resolves it for Codex-u498. |

---

## Stop criteria

A cycle exits early without producing a triage decision if:

- The open backlog is < 10 beads (use `bd ready` directly instead).
- `bd sync` is dirty / out of sync — risk of misclassification.
- The current user has > 0 in_progress beads claimed in another agent — wait for that work to land.

A bead is removed from the ladder if:

- It is closed by another skill or by hand.
- It is reassigned to another owner via `bd update`.
- It moves to `in_progress` under another agent.

---

## Notes

The master board is updated in step 7 of every cycle. It is the source-of-truth for what `/triage` will pick next; if the snapshot diverges from the actual `bd` state, the next cycle's classify step refreshes it.

Diff with denoise's `master.md`: triage doesn't track 12 cells (denoise's matrix). It tracks 5 rungs (the complexity ladder). Cycle history rows include the bead ID resolved or escalated, not just the cell.
